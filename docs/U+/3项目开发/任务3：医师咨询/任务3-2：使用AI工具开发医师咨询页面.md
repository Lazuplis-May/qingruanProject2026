任务详情

任务3-2：使用AI工具开发医师咨询页面

建议工时：

1

任务描述

**1. 任务描述**

本任务通过AI工具实现医师咨询界面设计，并通过DeepSeek&Cline优化其代码。

**2. 任务知识**

**知识点 ：**UI界面设计；

**重点 ：**使用AI工具完成首页界面设计；

**难点 ：**使用AI工具完成首页界面设计；

**3. 任务成果**

本任务成果为医师咨询界面设计。

示例如下：

![img](https://tecs-prod-static.obs.cn-north-1.myhuaweicloud.com/b6ede7dc667344c9b6cf219f83a9cd69%2Frichtext%2Fimage%2F20250415%2F0f840824961f49c0aa0a73345e7ecdc7.png)

图1 医师咨询界面设计图



任务指导

##### 1. 使用AI工具设计UI界面

在MasterGoAI生成界面，切换生成界面为“移动端”，输入医师咨询界面提示词，参考如下提示词：

```
帮我设计一个医师咨询界面，界面为医师咨询聊天框，最上方为医师咨询标题和退出按钮，上方为医师状态，显示医师头像、名称、课时、职位和在线状态。中间部分为双方的聊天对话信息，对话信息包括头像，名称，对话时间和对话内容，最下方为输入框，当输入框没有内容时则不显示发送按钮。
```

![img](https://tecs-prod-static.obs.cn-north-1.myhuaweicloud.com/43953f33804744cdb9e16c7869b1667e%2Frichtext%2Fimage%2F20250508%2Fc3e1ae9f9ae346a791fe2ce9c41efae6.png)

图1 设计UI界面

```
帮我设计一个医师咨询界面，界面为医师咨询聊天框，最上方为医师咨询标题和退出按钮，上方为医师状态，显示医师头像、名称、科室、职位和在线状态。中间部分为双方的聊天对话信息，对话信息包括头像，名称，对话时间和对话内容，最下方为输入框，当输入框没有内容时则不显示发送按钮。
```

选取一版比较满意的方案进行继续调整。

![img](https://tecs-prod-static.obs.cn-north-1.myhuaweicloud.com/b6ede7dc667344c9b6cf219f83a9cd69%2Frichtext%2Fimage%2F20250415%2F1ff122914d10428eac15ffa9f100c80d.png)

图2 设计UI界面

生成一版比较满意的界面后，点击右下方的按钮，展开代码，然后点击“复制代码”，并在项目工程中创建“chat”文件夹，在此文件夹下创建“chat.html”文件，将代码粘贴到此文件中。

![img](https://tecs-prod-static.obs.cn-north-1.myhuaweicloud.com/b6ede7dc667344c9b6cf219f83a9cd69%2Frichtext%2Fimage%2F20250415%2F265d0c7da64645c2a3d737e0621ce707.png)

图3 设计UI界面

####  

##### 2. 使用DeepSeek&Cline优化代码

1）优化素材资源

将图片链接下载到本地img目录下，然后重新引用图片素材。

2）优化界面设计

查看本地页面的运行效果，并根据实际情况优化界面设计，例如在最下方有一个添加按钮，需要删除，通过Cline删除按钮。

![img](https://tecs-prod-static.obs.cn-north-1.myhuaweicloud.com/b6ede7dc667344c9b6cf219f83a9cd69%2Frichtext%2Fimage%2F20250415%2F74493c2312a241c9a5752f12c34742a7.png)

图4 Cline生成代码

3）实现页面跳转功能

通过DeepSeek&Cline实现通过主页到聊天页面的跳转，并动态加载医生信息，参考以下提示词。

![img](https://tecs-prod-static.obs.cn-north-1.myhuaweicloud.com/b6ede7dc667344c9b6cf219f83a9cd69%2Frichtext%2Fimage%2F20250415%2F77a3ea6e14414423a6ee0ddac5d7bc39.png)

图5 Cline提示词

任务实现

由于每个人的提示词可能存在差异，AI工具再生成代码时也可能存在差异，本次项目不要求每人的界面内容完全相同，所以本任务代码实现仅供参考。

效果图如下：

![img](https://tecs-prod-static.obs.cn-north-1.myhuaweicloud.com/b6ede7dc667344c9b6cf219f83a9cd69%2Frichtext%2Fimage%2F20250416%2Ff1b9d40db4e940cda35553d456f0e66f.png)

修改/main/js/main.js代码，添加跳转功能，修改后的代码如下。

```
let docList = [];
// Helper functions for SessionStorage
function getCachedData(key) {
    const cached = sessionStorage.getItem(key);
    if (!cached) return null;
    
    const { data, timestamp } = JSON.parse(cached);
    // Cache expires after 1 hour (3600000 ms)
    if (Date.now() - timestamp > 3600000) {
        sessionStorage.removeItem(key);
        return null;
    }
    return data;
}

function setCachedData(key, data) {
    const cache = {
        data,
        timestamp: Date.now()
    };
    sessionStorage.setItem(key, JSON.stringify(cache));
}

// Load doctors data
async function loadDoctors() {
    try {
        const cacheKey = 'doctors_data';
        let data = getCachedData(cacheKey);
        
        if (!data) {
            data = await fetchSQLWorkflow('获取医生列表', 'current_user');
            setCachedData(cacheKey, data);
        }
        const container = document.getElementById('doctors-container');
        container.innerHTML = data.result.map(doctor => `
            <div class="doctor-card flex-shrink-0 w-32 h-56 bg-white rounded-lg p-3 flex flex-col">
                <div class="flex flex-col items-center space-y-3">
                    <div class="relative mb-3">
                        <img src="${doctor.image_url || '/img/doc1.png'}" 
                             alt="${doctor.doctor_name}" class="w-20 h-20 rounded-full object-cover border-2 border-blue-500">
                        <div class="absolute bottom-0 left-0 right-0 bg-white text-primary text-xs text-center py-[1px] font-normal">
                            ${doctor.title || '专科医师'}
                        </div>
                    </div>
                    <h3 class="text-sm font-semibold text-gray-800 text-center mb-2">${doctor.doctor_name}</h3>
                    <p class="text-xs text-gray-500 mb-3 text-center">${doctor.department || '内分泌科'}</p>
                    <button class="py-1 bg-white text-primary text-xs rounded-button btn consult-btn" 
                            data-doctor="${doctor.info_id}">
                        立即咨询
                    </button>
                </div>
            </div>
        `).join('');
        docList = data.result;

    } catch (error) {
        console.error('加载医生数据失败:', error);
        document.getElementById('doctors-container').innerHTML = 
            '<div class="text-center py-10 w-full text-red-500">加载医生数据失败，请稍后重试</div>';
    }
}

// Load articles data
async function loadArticles() {
    try {
        const cacheKey = 'articles_data';
        let data = getCachedData(cacheKey);
        
        if (!data) {
            data = await fetchSQLWorkflow('查询3篇文章信息', 'current_user');
            setCachedData(cacheKey, data);
        }
        const container = document.getElementById('articles-container');
        console.log('Fetched doctors:', data);

        container.innerHTML = data.result.map(article => `
            <div class="article-item px-4 py-3">
                <div class="flex">
                    <div class="w-20 h-20 rounded-lg overflow-hidden mr-3">
                        <img src="${article.cover_url || '/img/a1.jpg'}" 
                             alt="${article.title}" class="w-full h-full object-cover">
                    </div>
                    <div class="flex-1">
                        <h3 class="text-sm font-semibold text-gray-800 mb-1">${article.title}</h3>
                        <p class="text-xs text-gray-500 mb-2 line-clamp-2">${article.content.substring(0, 20)+"..." || '暂无简介'}</p>
                        <div class="flex items-center text-xs text-gray-400">
                            <i class="fas fa-eye mr-1"></i>
                            <span>${article.views || '0'} 浏览</span>
                        </div>
                    </div>
                </div>
            </div>
        `).join('');
    } catch (error) {
        console.error('加载文章数据失败:', error);
        document.getElementById('articles-container').innerHTML = 
            '<div class="text-center py-10 text-red-500">加载文章数据失败，请稍后重试</div>';
    }
}

// Load diabetes types data
async function loadTypes() {
    try {
        const cacheKey = 'diabetes_types_data';
        let data = getCachedData(cacheKey);
        
        if (!data) {
            data = await fetchSQLWorkflow('获取糖尿病类型', 'current_user');
            setCachedData(cacheKey, data);
        }
        const container = document.getElementById('types-container');
        
        container.innerHTML = data.result.map(type => `
            <div class="category-item bg-white rounded-lg overflow-hidden">
                <div class="h-24 overflow-hidden">
                    <img src="${type.img || '/img/t1.jpg'}" 
                         alt="${type.type_name}" class="w-full h-full object-cover">
                </div>
                <div class="p-3">
                    <h3 class="text-sm font-semibold text-gray-800 mb-1">${type.type_name}</h3>
                    <p class="text-xs text-gray-500 line-clamp-2">${type.pathogenesis || '暂无描述'}</p>
                </div>
            </div>
        `).join('');
    } catch (error) {
        console.error('加载糖尿病类型数据失败:', error);
        document.getElementById('types-container').innerHTML = 
            '<div class="text-center py-10 col-span-2 text-red-500">加载糖尿病类型数据失败，请稍后重试</div>';
    }
}

// Handle consultation button clicks
function setupConsultButtons() {
    document.addEventListener('click', (e) => {
        if (e.target.classList.contains('consult-btn')) {
            const id = e.target.getAttribute('data-doctor');
            console.log(id)
            docList.forEach((doc)=>{
                if(doc.info_id == id){
                    localStorage.setItem('currentDoctor', JSON.stringify(doc));
                } 
            })
            // 跳转到聊天页面并传递医生信息
            top.location.href = `/chat/chat.html`;
        }
    });
}

// Load all data when page loads
document.addEventListener('DOMContentLoaded', () => {
    loadDoctors();
    loadArticles();
    loadTypes();
    setupConsultButtons();
});
```

新建chat目录，在chat目录中新建chat.html，代码参考如下：

```
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>医师咨询</title>
    <script src="/js/tailwindcss.js"></script>
    <script src="/js/api.js"></script>
    <script src="js/chat.js"></script>
    <link rel="stylesheet" href="css/chat.css">
    <link rel="stylesheet" href="/css/all.min.css">
</head>
<body class="bg-gray-50 font-sans">
    <div class="flex flex-col h-screen">
        <!-- 顶部导航栏 -->
        <header class="fixed top-0 left-0 right-0 z-10 shadow-sm bg-white">
            <div class="flex items-center justify-between px-4 py-3">
                <button onclick="window.history.back()">
                    <i class="fas fa-chevron-left text-lg"></i>
                </button>
                <h1 class="text-lg font-medium">医师咨询</h1>
                <button >
                    <i class="fa-solid fa-trash  text-gray-600 text-lg"></i>
                </button>
            </div>
        </header>

        <!-- 医师信息区域 -->
        <div class="pt-16 pb-4 bg-white shadow-md rounded-b-lg mx-4">
            <div class="flex items-center px-4">
                <div class="relative">
                    <img src="/img/doc1.jpg" class="doctor-avatar object-cover" alt="医师头像">
                    <div class="absolute bottom-0 right-0 bg-white p-0.5 rounded-full">
                        <div class="status-dot bg-green-500"></div>
                    </div>
                </div>
                <div class="ml-3">
                    <div class="flex items-center">
                        <h2 class="font-bold text-gray-900">张明远 医师</h2>
                        <span class="ml-2 text-xs text-gray-500">在线</span>
                    </div>
                    <p class="text-sm text-gray-500">心血管内科主任医师 · 15年经验</p>
                </div>
            </div>
        </div>

        <!-- 聊天内容区域 -->
        <div id="messages-container" class="flex-1 overflow-y-auto px-4 py-2 space-y-4">
            <!-- 对方消息 -->
            <div class="flex items-start">
                <img src="/img/doc1.jpg" 
                     class="message-avatar object-cover" alt="医师头像">
                <div class="ml-2">
                    <div class="flex items-center">
                        <span class="text-sm font-medium text-gray-900">张明远 医师</span>
                        <span class="ml-2 text-xs text-gray-400">10:25</span>
                    </div>
                    <div class="mt-1 bg-white rounded-lg shadow-sm px-3 py-2 chat-bubble">
                        <p class="text-sm text-gray-800">您好，我是张医生。请问有什么可以帮您的吗？</p>
                    </div>
                </div>
            </div>

            <!-- 我的消息 -->
            <div class="flex items-start justify-end">
                <div class="mr-2 text-right">
                    <div class="flex items-center justify-end">
                        <span class="text-xs text-gray-400">10:26</span>
                        <span class="ml-2 text-sm font-medium text-gray-900">我</span>
                    </div>
                    <div class="mt-1 bg-primary rounded-lg shadow-sm px-3 py-2 chat-bubble">
                        <p class="text-sm text-white">张医生您好，我最近一周经常感到胸闷，特别是在晚上睡觉的时候。</p>
                    </div>
                </div>
                <img src="/img/user.jpg" 
                     class="message-avatar object-cover" alt="我的头像">
            </div>

            <!-- 对方消息 -->
            <div class="flex items-start">
                <img src="/img/doc1.jpg" 
                     class="message-avatar object-cover" alt="医师头像">
                <div class="ml-2">
                    <div class="flex items-center">
                        <span class="text-sm font-medium text-gray-900">张明远 医师</span>
                        <span class="ml-2 text-xs text-gray-400">10:27</span>
                    </div>
                    <div class="mt-1 bg-white rounded-lg shadow-sm px-3 py-2 chat-bubble">
                        <p class="text-sm text-gray-800">了解。请问胸闷的程度如何？是轻微的压迫感还是明显的疼痛？</p>
                    </div>
                </div>
            </div>

            <!-- 我的消息 -->
            <div class="flex items-start justify-end">
                <div class="mr-2 text-right">
                    <div class="flex items-center justify-end">
                        <span class="text-xs text-gray-400">10:28</span>
                        <span class="ml-2 text-sm font-medium text-gray-900">我</span>
                    </div>
                    <div class="mt-1 bg-primary rounded-lg shadow-sm px-3 py-2 chat-bubble">
                        <p class="text-sm text-white">主要是压迫感，有时候会感觉呼吸不太顺畅，但不是很疼。</p>
                    </div>
                </div>
                <img src="/img/user.jpg" 
                     class="message-avatar object-cover" alt="我的头像">
            </div>

            <!-- 对方消息 -->
            <div class="flex items-start">
                <img src="/img/doc1.jpg" 
                     class="message-avatar object-cover" alt="医师头像">
                <div class="ml-2">
                    <div class="flex items-center">
                        <span class="text-sm font-medium text-gray-900">张明远 医师</span>
                        <span class="ml-2 text-xs text-gray-400">10:30</span>
                    </div>
                    <div class="mt-1 bg-white rounded-lg shadow-sm px-3 py-2 chat-bubble">
                        <p class="text-sm text-gray-800">我明白了。这种情况持续多久了？有没有其他伴随症状，比如咳嗽、发热或者心悸？</p>
                    </div>
                </div>
            </div>
        </div>

        <!-- 底部输入区域 -->
        <div class="fixed bottom-0 left-0 right-0 bg-white border-t border-gray-200 px-4 py-2">
            <div class="flex items-center input-container">
                <div class="flex-1 bg-gray-100 rounded-full px-4 py-2">
                    <input type="text" placeholder="输入消息..." class="w-full bg-transparent outline-none text-sm" id="messageInput">
                </div>
                <button class="ml-2 bg-primary text-white rounded-full send-btn hidden" id="sendBtn">
                    <i class="fas fa-paper-plane text-white text-sm"></i>
                </button>
            </div>
        </div>
    </div>
</body>
</html>
```

在chat目录中创建css目录，并创建chat.css文件，代码如下：

```
.message-avatar {
    width: 40px;
    height: 40px;
    border-radius: 9999px;
    border: 2px solid #304FFF;
}
.doctor-avatar {
    width: 54px;
    height: 54px;
    border-radius: 9999px;
    border: 2px solid #304FFF;
}
.status-dot {
    width: 8px;
    height: 8px;
    border-radius: 9999px;
}
.chat-bubble {
    max-width: 280px;
}
.input-container {
    min-height: 48px;
}
.send-btn {
    width: 32px;
    height: 32px;
    display: flex;
    justify-content: center;
    align-items: center;
}
i {
    display: flex;
    justify-content: center;
    align-items: center;
}
#messages-container{
    margin-bottom: 65px;
}
.msg {
    white-space: pre-line;
    padding: 5px;
}
details{
    padding-bottom: 2px;
}
```

在chat目录下新建js目录，并在此目录下新建chat.js文件，代码如下：

```
let doc = null
tailwind.config = {
    theme: {
        extend: {
            colors: {
                primary: '#304FFF',
                secondary: '#10B981'
            },
            borderRadius: {
                'none': '0px',
                'sm': '2px',
                DEFAULT: '4px',
                'md': '8px',
                'lg': '12px',
                'xl': '16px',
                '2xl': '20px',
                '3xl': '24px',
                'full': '9999px',
                'button': '4px'
            }
        }
    }
}
window.onload = function() {

    const messageInput = document.getElementById('messageInput');
    const sendBtn = document.getElementById('sendBtn');
    messageInput.addEventListener('input', function () {
        if (this.value.trim() !== '') {
            sendBtn.classList.remove('hidden');
        } else {
            sendBtn.classList.add('hidden');
        }
    });

    sendBtn.addEventListener('click', function () {
        const message = messageInput.value.trim();
        if (message !== '') {
            console.log('发送消息:', message);
            messageInput.value = '';
            sendBtn.classList.add('hidden');
        }
    });

    messageInput.addEventListener('keypress', function (e) {
        if (e.key === 'Enter' && this.value.trim() !== '') {
            const message = messageInput.value.trim();
            console.log('发送消息:', message);
            messageInput.value = '';
            sendBtn.classList.add('hidden');
        }
    });
}
```