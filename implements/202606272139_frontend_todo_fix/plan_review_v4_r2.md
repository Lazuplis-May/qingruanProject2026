# 第4轮规划审查报告 v4-r2

> **审查对象**: `task_v4.md` (770行) -- 修订后的第4轮任务文件
> **前次审查**: `plan_review_v4_r1.md` (4条拒绝理由，3项批准条件)
> **审查日期**: 2026-06-27
> **审查人**: Plan Reviewer

---

## 0. 前次拒绝理由逐条复查

### 拒绝理由 1: task_v4.md 缺失 --> 已修正

**前次状态**: v4 仅有 plan.md 一行概要描述（`G7/G8/G12 + S10 + G3/G6`），无可执行任务分解。

**当前状态**: task_v4.md 已创建，共 770 行，包含 5 个任务组（G1-G5）的完整分解。每个任务组均包含:
- 问题编号与严重程度
- 预估工时
- 前置依赖（含依赖性质说明: 硬依赖/建议依赖）
- 涉及文件清单（含操作类型: 新建/修改）
- 具体修改描述（含完整代码模板和行号引用）
- 3-5 条可操作验收标准
- 边界条件

**判定: PASS** -- task_v4.md 的粒度达到甚至超过了 v1 (317行)、v2 (298行) 的标准，与 v3 (1315行) 相比虽篇幅较短（P3 6项 vs P0+P1 2项但内容密度高），完全可执行。

---

### 拒绝理由 2: 范围定义不一致 --> 已修正

**前次状态**: task_v3.md 第1315行承诺 v4 将处理"v3 可推迟项的完整交付"（4项实际需交付任务，约8h），但 plan.md v4 行完全未提及。

**当前状态**: task_v4.md 第13-32行设立了明确的"范围裁定"节，包含:

1. **明确裁决**: 采用方案A（严格按 plan.md P3 范围），v3 可推迟项全部推迟至 v5。
2. **三条裁决理由**:
   - 工时膨胀风险（8-13h → 16-21h，接近 v3 简化版规模）
   - 文件冲突隔离（断线重连和多医生路由涉及 chatStore.ts，与 P3 层文件无共享）
   - v5 已有容量（~15项 7-12h + 推迟项 ~8h = 15-20h，仍在可控范围）
3. **完整推迟清单**（表格形式，6行，标注来源、v3简化交付、v5完整交付、工时）
4. **G16 合并说明**: G16（marked async 兼容性注释）已纳入 G2 任务组一并处理，不增加额外工时。

**判定: PASS** -- 范围定义已统一，裁决理由充分且透明。task_v4.md 明确标注"plan.md 需同步更新以反映此范围变更"——这是对 plan.md 的修改提醒，不影响 task_v4.md 自身的完整性。

---

### 拒绝理由 3: 内部依赖关系未定义 --> 已修正

**前次状态**: P3 层 6 个问题存在文件共享关系（sanitize.ts 被 G12/S10 共享，Punch.vue 被 4 个任务修改），但 plan.md 未给出任何执行顺序或批处理建议。

**当前状态**: task_v4.md 提供了三层依赖关系定义:

**第一层 — 可视化依赖图**（第36-56行）:
```
[G1] sanitize.ts 一站化 (G12+S10)
  └─→ [G2] useMarkdown.ts 抽取 (G7+G16)
        └─→ [G3] errorMessage.ts 抽取 (G8)
              └─→ [G4] + [G5] 可并行
```

**第二层 — 依赖性质表**（第58-65行）:
| 依赖 | 方向 | 性质 | 说明 |
| G12→S10 | G12先于S10 | 硬依赖（同一文件） | 合并为G1一站化 |
| G1→G2 | G1先于G2 | 建议依赖（避免回修） | G2使用G1的sanitizeHtml |
| G1+G2+G3→G4+G5 | 工具抽取先于UI新增 | 建议依赖（文件级协调） | 稳定import结构 |

**第三层 — 执行顺序建议**（第68-74行）:
- 单人串行: G1 → G2 → G3 → G4 → G5
- 二人并行: 开发者A (G1+G2+G3) → 开发者B (G4+G5)

此外，第64-66行还额外说明了"四人修改 Punch.vue 的区域不重叠"，在第722-740行的文件修改清单中进一步展开为具体的修改区域映射（import区 / 函数定义区 / 统计卡模板区 / 筛选区）。

**判定: PASS** -- 依赖关系标注清晰、分层、有据。硬依赖/建议依赖的区分有助于执行者判断何时可以并行、何时必须串行。

---

### 拒绝理由 4: 工具抽取与安全加固的鸡-蛋依赖未标注 --> 已修正

**前次状态**: G7 (useMarkdown.ts) 使用裸 `DOMPurify.sanitize()`，S10 (安全加固) 创建 `sanitizeHtml()` 后需要回修 G7 的产出。这个回修循环未被识别。

**当前状态**: 从三个层面解决了鸡-蛋依赖:

1. **代码层面**: G2 (useMarkdown.ts) 的代码模板（第233-263行）中 `renderMarkdown()` 直接 `import { sanitizeHtml } from '@/utils/sanitize'`，并在注释中标注 fallback: "如果 G1 未完成，临时使用裸 DOMPurify.sanitize() 并在注释中标注 TODO"。

2. **依赖标注**: G2 的前置依赖明确标注为 "G1（sanitize.ts 就绪 -- 建议依赖。若 G1 未完成则可先行抽取使用裸 DOMPurify.sanitize()，后续回修 1-2 行。）"（第218行）。

3. **风险提示**: 风险第2项（第748-749行）明确标注: "如果 G2 先于 G1 执行，G1 完成后需回修 useMarkdown.ts 的 1-2 行代码。缓解措施: 严格按 G1→G2 顺序执行"。

4. **补充建议跟踪**: S5（第765行）标注"G12+S10→G7 顺序优于诊断报告的 S10 依赖 G7 顺序"，已采纳。

**判定: PASS** -- 鸡-蛋依赖已被识别、标注，且通过执行顺序（G1→G2）和代码设计（useMarkdown.ts 直接使用 sanitizeHtml）从根本上消除。

---

## 1. 粒度标准审查

### 1.1 任务组粒度对比

| 指标 | v1 (6任务) | v2 (5任务) | v3 (7任务组) | v4-r2 (5任务组) | 标准 |
|------|:--------:|:--------:|:----------:|:-------------:|:---:|
| 总行数 | 317 | 298 | 1315 | 770 | >=200 |
| 每组平均行数 | ~53 | ~60 | ~188 | ~154 | >=80 |
| 每组验收标准数 | 3-5 | 3-5 | 4-7 | 5 | >=3 |
| 每组涉及文件清单 | 有 | 有 | 有 | 有 | 必须 |
| 每组边界条件 | 有 | 有 | 有 | 有 | 必须 |
| 跨轮次依赖确认 | 有 | 无 | 有 | 有 | 建议 |
| 风险矩阵 | 有 | 有 | 有 | 有 | 建议 |

**判定: PASS** -- v4-r2 在所有维度上均达到或超过前几轮的标准。5个任务组每个均超过100行，验收标准均为5条（达到上限），边界条件覆盖全面。

### 1.2 关键要素完整性

逐组检查:

**G1 (sanitize.ts 一站化, 第78-209行, 131行)**:
- [x] 问题编号+严重程度 (G12+S10, 低/中)
- [x] 预估工时 (3-4h)
- [x] 前置依赖 (无)
- [x] 批处理理由 (共享 sanitize.ts，避免 git 合并冲突)
- [x] 涉及文件清单 (4文件，含操作类型)
- [x] 具体修改描述 (4个子步骤，含完整代码)
- [x] 边界条件 (6条，含空值防御、协议白名单、标签白名单)
- [x] 验收标准 (5条，含 XSS 注入测试、编译验证)

**G2 (useMarkdown.ts 抽取, 第213-312行, 99行)**:
- [x] 问题编号+严重程度 (G7+G16, 低/低)
- [x] 预估工时 (1.5-2.5h)
- [x] 前置依赖 (G1 建议依赖)
- [x] 合并理由 (G16 注释在抽取后统一添加更高效)
- [x] 涉及文件清单 (3文件)
- [x] 具体修改描述 (含完整 composable 代码+G16注释)
- [x] 边界条件 (4条，含 null/undefined/number/空字符串)
- [x] 验收标准 (5条)

**G3 (errorMessage.ts 抽取, 第315-409行, 94行)**:
- [x] 问题编号+严重程度 (G8, 低)
- [x] 预估工时 (1-1.5h)
- [x] 前置依赖 (无硬依赖，建议G2之后)
- [x] 涉及文件清单 (3文件)
- [x] 具体修改描述 (含完整工具函数+fallback行为分析)
- [x] 边界条件 (5条，含 null/undefined/字符串/Error/Axios错误)
- [x] 验收标准 (5条)

**G4 (Punch.vue 环形图, 第413-556行, 143行)**:
- [x] 问题编号+严重程度 (G3, 中/低)
- [x] 预估工时 (2-3h)
- [x] 前置依赖 (建议G2+G3之后)
- [x] 涉及文件清单 (1文件，含修改区域行号)
- [x] 具体修改描述 (含 SVG 模板+computed+CSS+动画说明)
- [x] 边界条件 (6条，含 null/total=0/completed>total/0%/动画/响应式)
- [x] 验收标准 (5条)

**G5 (Punch.vue 刷新按钮, 第560-704行, 144行)**:
- [x] 问题编号+严重程度 (G6, 中/低)
- [x] 预估工时 (1-2h)
- [x] 前置依赖 (建议G2+G3之后)
- [x] 涉及文件清单 (1文件)
- [x] 具体修改描述 (含模板+script+CSS+清理逻辑)
- [x] 边界条件 (5条，含防双击/加载反馈/筛选保持/错误处理/组件卸载)
- [x] 验收标准 (5条)

**判定: PASS** -- 所有5个任务组均满足前次审查设定的批准条件粒度要求。

---

## 2. 依赖关系清晰度审查

### 2.1 依赖标注矩阵

| 依赖关系 | 标注位置 | 性质标注 | 理由说明 | 缓解措施 |
|---------|:------:|:------:|---------|:------:|
| G12→S10 (同文件) | G1 批处理理由 (第83行) | 硬依赖 | 共享 sanitize.ts | 合并为G1一站化 |
| G1→G2 (sanitize→useMarkdown) | G2 前置依赖 (第218行) | 建议依赖 | 避免回修1-2行 | 严格G1→G2顺序 |
| G1+G2+G3→G4+G5 | G4/G5 前置依赖 (第418/565行) | 文件级建议依赖 | 稳定import结构 | 同开发者顺序执行 |
| G2↔G3 (同文件区域) | G3 前置依赖 (第320-321行) | 文件级协调 | 共享Punch/LifePlan import区 | 建议G2→G3顺序 |

### 2.2 跨轮次依赖

task_v4.md 第708-718行的跨轮次依赖表:

| 前置依赖 | 来自轮次 | 状态 | 影响评估 |
|---------|:------:|:----:|---------|
| S9 (fetchAnalysis 竞态) | v1 | 已完成 | G5 刷新按钮自动受益 |
| S3 (默认近30天筛选) | v1 | 已完成 | G5 fetchList 自动受益 |
| G14 (success 拦截器) | v2 | 已完成 (phase1) | G5 API调用自动受益 |
| S5b-1/S5b-2 (chatStore SSE) | v3 | 已完成 | 无交叉依赖 |
| marked v12 | — | 已安装 | G2 API兼容 |

**判定: PASS** -- 依赖标注覆盖了组内（G12→S10→G7→G8）、组间（G1→G2→G3→G4+G5）、跨轮次（v1/v2/v3 产出）三个层次，每种依赖均标注了性质（硬依赖/建议依赖/文件级协调）和理由。

---

## 3. 补充审查项

### 3.1 验收标准可操作性与前次审查建议跟踪

前次审查补充建议 S1-S5 的采纳情况:

| 编号 | 建议 | 采纳状态 | task_v4.md 对应位置 |
|:--:|------|:------:|------------------|
| S1 | G12+S10 合并为一个任务组 | **已采纳** | G1 一站化完成 (第78-209行) |
| S2 | renderMarkdown 直接使用 sanitizeHtml | **已采纳** | G2 代码模板使用 `import { sanitizeHtml }` (第235行) |
| S3 | G14-phase2 切换状态确认 | 标注待办 | 风险提示第5项 (第754行)，建议 v5 安排 |
| S4 | Punch.vue 修改由同一开发者按序执行 | **已采纳** | 风险提示第3项 (第750行)，执行顺序建议 |
| S5 | G12+S10→G7 优于诊断报告顺序 | **已采纳** | 执行顺序 G1→G2，偏差理由在 G2 前置依赖中注明 |

**判定: PASS** -- 5条补充建议中 4 条已完全采纳，1 条（S3 G14-phase2）标注为 v5 待办（合理，因 G14 phase2 切换涉及后端语义确认，不宜在 v4 强行安排）。

### 3.2 Punch.vue 多任务修改区域分析

task_v4.md 第64-66行和第734-740行提供了两次 Punch.vue 修改区域分析:

| 任务组 | 修改区域 | 行号范围 | 是否重叠 |
|:------:|---------|:------:|:------:|
| G1 | import 区 | `<script setup>` 顶部 | 与 G2/G3 共享 import 区 |
| G2 | 删除 safeAnalysisHtml | 第55-60行 | 与 G1/G3 共享 import 区 |
| G3 | 删除 getErrorMessage | 第63-77行 | 与 G1/G2 共享 import 区 |
| G4 | 统计卡模板 | 第192-209行 | **不重叠** |
| G5 | 筛选区模板 | 第270-304行 | **不重叠** |

分析结论明确（第66行）: "修改区域不重叠意味着理论上可并行，但建议工具抽取先完成以稳定 import 结构。" 这个分析既诚实（承认理论上可并行）又务实（建议串行降低风险）。

**判定: PASS** -- Punch.vue 的多任务修改已充分分析，区域隔离明确，协调策略合理。

### 3.3 G3 (errorMessage.ts) 行为变更风险

task_v4.md G3 的边界条件（第394-401行）识别了一个关键行为差异:

> Punch.vue 本地版本（第63-77行）额外包含了 `if (!err) return ''` 的空值判断。统一版本中...空值 null/undefined 会走到 fallback 返回 `'操作失败，请稍后重试'`。

并在修改描述第 365 行和第 400-401 行给出了处理指导:
- 第365行: "如果 Punch.vue 中存在依赖 `getErrorMessage(null)` 返回 `''` 的 UI 逻辑，需检查对应模板中的 `v-if` 条件是否用独立变量（如 `listError`）而非 `getErrorMessage(listError)` 的返回值来判断是否展示错误提示。"
- 第400-401行: "如果 Punch.vue 中存在依赖此行为的模板逻辑，需在调用处调整。"

**判定: PASS** -- 行为变更已被识别并给出了具体的排查指引。不是笼统的"注意兼容性"而是精确到变量名（listError）和模板条件的检查点。

### 3.4 G4 环形图设计依据

task_v4.md G4 的设计依据引用了:

1. 设计文档 4.3 节 Punch.vue 流程图（第3797行）: `分析数据展示 完成率环形图 近7天趋势柱状图`
2. 诊断报告第406行: "CSS 叠柱在数据可视化效果上可接受" —— 作为不修改趋势图的依据

纯 SVG 方案（不引入第三方库）的决策合理，避免了包体积膨胀。动画使用 CSS transition 而非 JS animation 的说明（第536行）体现了对性能的考量。

**判定: PASS** -- 技术决策有据，方案完整（含 SVG 模板、computed、CSS、动画说明）。

---

## 4. 风险矩阵审查

task_v4.md 第744-755行的 5 项风险:

| 风险 | 概率 | 影响 | 缓解措施 | 评价 |
|------|:---:|:---:|---------|------|
| G1 sanitize.ts 首次提交质量 | 低 | 中 | G1 完成后在 LifePlan 验证 Markdown 渲染 | 可操作 |
| G1-G2 鸡-蛋依赖 | 中 | 低 | 严格 G1→G2 顺序 | 已通过代码设计消除 |
| Punch.vue 5组修改合并冲突 | 中 | 中 | 同开发者按序执行 | 务实 |
| DOMPurify 白名单过严 | 低 | 高 | G1 后三页面渲染回归验证 | 关键防护 |
| G14-phase2 切换时机 | 低 | 低 | v5 安排 | 非阻塞 |

**判定: PASS** -- 风险矩阵覆盖了技术风险（白名单过严、鸡-蛋依赖）和流程风险（合并冲突、提交质量），每项有概率、影响、缓解措施。风险#4（白名单过严，低概率高影响）的缓解措施（渲染回归验证）是最重要的防线，已明确。

---

## 5. 前次审查建议跟踪（补充）

task_v4.md 第758-766行的"补充建议"表格追踪了前次审查报告的 5 条 S 类建议。第708-718行追踪了跨轮次依赖的 5 项前置条件。第13-32行追踪了 v3 可推迟项的范围裁决。三处跟踪覆盖了前次审查报告提出的所有未决问题。

唯一未闭环的遗留项: **plan.md 尚未同步更新**。task_v4.md 第32行标注 "plan.md 需同步更新以反映此范围变更"。这不影响 task_v4.md 自身的批准——plan.md 的更新可在 task_v4.md 批准后、v4 执行前完成。

---

## 6. 审查结论

### 判定: **APPROVED** -- 所有 4 条拒绝理由已修正，task_v4.md 达到可执行标准

### 批准依据

1. **task_v4.md 已创建**（770行，5个任务组），粒度超过 v1/v2 标准，每组均含问题编号、工时、依赖、文件清单、具体修改、验收标准、边界条件。

2. **范围不一致已解决**: 明确采用方案A（严格按 plan.md P3 范围），v3 可推迟项全部推迟至 v5，裁决理由（工时膨胀/文件冲突/v5容量）充分且透明。

3. **内部依赖关系已标注**: 三层依赖定义（可视化图 + 性质表 + 执行顺序），覆盖硬依赖/建议依赖/文件级协调，同时提供了单人串行和二人并行的执行方案。

4. **鸡-蛋依赖已消除**: G2 代码模板直接使用 G1 的 `sanitizeHtml()`，执行顺序 G1→G2 从根本上消除回修循环。风险提示中保留了如果乱序执行的应对方案。

### 遗留建议（非阻塞，供 v4 执行前参考）

| 编号 | 建议 | 优先级 |
|:--:|------|:----:|
| L1 | 同步更新 plan.md 第4.1节 v4 行，反映范围裁决（v3 可推迟项推迟至 v5）及 G16 并入 G2 的合并 | 中 |
| L2 | G1 完成后立即执行三个页面的 Markdown 渲染回归验证（对比替换前后 HTML 输出），如发现 DOMPurify 白名单过严则调整 ALLOWED_TAGS | 高 |
| L3 | v4 开始前确认 G14 当前仍为 console.warn 阶段（phase1），避免意料外的 Promise.reject 影响 | 低 |
| L4 | 如果采用二人并行模式，开发者B 在开发者A 完成 G3 之后再开始 G4+G5（而非在 G3 完成前 fork） | 中（仅并行场景） |

---

*审查报告结束。task_v4.md 批准通过，可以进入执行阶段。*
