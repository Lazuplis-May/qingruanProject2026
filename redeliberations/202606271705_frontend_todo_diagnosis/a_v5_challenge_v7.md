# 诊断质询报告（v7）

## 质询结果

LOCATED

## 逐维度审查

### 1. 证据充分性

**[通过]** 全部关键证据链经实际代码抽查验证一致。抽查项包括：(a) `LifePlan.vue` 第76行 `prefillFromRiskForm()` 确实调用了 `riskForm.loadFromStorage()`，与诊断S4第113行描述一致；(b) `LifePlan.vue` 第77-81行仅读取 `riskForm.formData`（age/gender/height/weight），未触及 `riskForm.result`，与诊断S4第107行描述一致；(c) `punchStore.ts` 第125-135行 `fetchAnalysis()` 无 `requestId` 快照保护，与诊断S9第290行描述一致；(d) `homeStore.ts` 第44-48行使用 `Promise.allSettled` 并行三个请求，第50-55行通过 `status === 'fulfilled'` 正确区分成功/失败路径，与诊断G18第602行描述一致；(e) 设计文档 `2_detailed_design_v3.md` 第2373行、第3543行明确规定了 `fetch + ReadableStream` SSE 消费方式，`1_requirements_analysis_v1.md` 第616行解释了不使用 EventSource 的原因（不支持自定义请求头，无法携带 JWT Token），与v7修订的S5b-1技术方案修正依据一致。

**[通过]** S5b chatStore 骨架状态的代码验证（S5b-1修复建议第139行 `conversations = ref([])` 空数组、`abortActiveConnection()` 空函数体、`sse.ts` 类型完全未被引用）经确认已在v5阶段通过实际文件读取完成，证据来源明确。

**[通过]** S5a 降级方案的分页行为不确定性已显式标注为"待验证项"（第133行），`Article` 类型缺少 `content` 字段的限制已明确审计（第125-128行），设计意图与类型约束的张力已作为v7新增分析段落处理。诊断未将未验证假设伪装为已确认事实。

**[问题-轻微]** S5a "设计意图与类型约束的张力分析"（第125-128行）识别了两种可能（设计文档预期了独立详情API vs 设计文档未考虑类型约束），但未建议查阅设计文档的API路由定义章节（`2_detailed_design_v3.md` 中应包含 `GET /api/articles/:id` 的路由规格定义）来消除不确定性。诊断结论"无法从现有文档中确定答案"的覆盖面可扩展至此途径。

**[问题-轻微]** G18 v7新增的 `homeStore.fetchHomeData()` 并行场景 page instance token 实现（第602-612行）分析了 `Promise.allSettled` 的整体丢弃策略，但未说明 `retryDoctors`/`retryArticles`/`retryTypes` 三个重试函数是否会错误地递增 `pageInstanceId`——如果实现者在重试函数中也递增了此计数器，将导致并行页面请求的响应被意外丢弃。由于诊断推荐的实现模式（仅在 `fetchHomeData()` 入口递增一次）天然避免了此问题，重试函数不应接触 `pageInstanceId`，但诊断未主动标注此注意事项。

### 2. 逻辑完整性

**[通过]** 从42项问题现象到6个根因的因果链完整且自洽。根因1（状态管理未对齐）→ S1/S2；根因2（路由拆分未跟进）→ S5a/S5b/S6；根因3（筛选联动遗漏）→ S3/S7；根因4（跨模块接收不完整）→ S4/S11；根因5（代码组织缺乏规范）→ G7-G9/G12等；根因6（API响应处理规范缺失）→ G14。每个根因到受影响问题的映射明确（第7节映射表），无循环依赖或矛盾。

**[通过]** v7修订解决了此前版本存在的三个逻辑不一致：(a) S5b-1 修复建议与设计文档的 SSE 实现方案矛盾——EventSource 无法携带 JWT Token 通过认证，已全面修正为 fetch+ReadableStream；(b) P2优先级层与P1批处理组之间的依赖矛盾——S9定级P2但为P1批处理组S3+S7的前置依赖，v7做出最终裁决将S9上调至P1；(c) S5a 降级方案在未验证分页行为时被标记为"可用"，v7修正为"条件可用"并标注待验证项。

**[通过]** 修复间交互风险分析（8.3(e)节）覆盖了三组关键交叉影响（G14↔S9、S8↔S1/S2、G7↔G14），每组标注了共享组件、交互性质和建议修复顺序。8.2节优先级表的依赖列和批处理组批注使修复者可无歧义地确定执行顺序。

**[问题-轻微]** S8 修复建议中的 BroadcastChannel 跨标签页同步代码示例（第258-265行）在 `onmessage` 处理器中检查 `token.value` 非空后才执行同步。此检查的设计意图是防止 Store 未完全水合时执行状态同步，但它同时阻止了新标签页（token为null）接收来自已登录标签页的登录广播——此时 `token.value` 为 null 意味着条件 `token.value` 为 falsy，同步被跳过，而新标签页正是需要接收此同步的目标。`clearAuth` 广播方向不受影响（因为接收方 token 非空时检查通过）。由于该代码段明确标注为"可选增强"且核心修复可独立交付，此矛盾不影响诊断主体结论的可靠性。

### 3. 覆盖完备性

**[通过]** todo.md 中的全部42项问题（13严重 + 29一般）均有逐项诊断，无遗漏。每项包含诊断结论、设计依据、代码证据、因果链、影响范围、修复建议和验证方法。

**[通过]** 诊断覆盖了与设计文档的三种偏差类型：功能遗漏（S1-S5/S7/G6/G3，第4.1节表）、设计偏差（S6/G28/G2/G5/G1，第4.2节表）、设计内部矛盾（S8/S13/G4/G2，第4.3节表）。技术可行性评估覆盖全部42项问题，结论均为可实现、无阻塞性技术障碍。

**[通过]** 修复优先级排序（第8.2节）覆盖了全部需要代码修复的问题，从P0到P4按影响面递减排列，含前置依赖标注和批处理组建议。无需代码修改的条目（G4/G5/S12/S13）已显式分离并标注行动类型。

**[问题-轻微]** S5b-1 修复建议（第152行）规定 conversation_id 存储在 localStorage 键 `qrzl_conv_${doctorId}` 中。诊断未讨论此存储策略与 S8 token 迁移（从 localStorage 到 sessionStorage）之后的交互——conversation_id 将成为 authStore 相关的唯一保留在 localStorage 中的持久化数据，且 `clearAuth()` 未覆盖其清理。在登出后其他用户登录同一浏览器的场景下，残存的 conversation_id 可能导致对话上下文跨用户泄露。这一缺陷与S1/S2缓存清理问题（已在v4中通过"与其他修复的交互"子节解决）属同类模式，但S5b-1作为新增存储键未纳入同样的交互分析框架。由于此问题属于修复方案的完整性范畴（非根因诊断范畴），且S5b-1的修复本身已标记为复杂任务（高复杂度、需从头构建），认定为轻微。

## 质询要点

无严重或一般问题。上述三个轻微问题均不改变诊断报告的根因定位和证据链完整性：

1. **S5a 设计文档API路由章节查阅建议（轻微）**: 诊断正确识别了不确定性但未给出最直接的消除路径。不影响诊断结论——"设计意图待确认"这个判断本身是正确的。

2. **G18 retry 函数与 pageInstanceId 的交互标注（轻微）**: 诊断推荐的实现模式已隐式避免了问题，但缺少显式的注意事项。不影响修复者正确实施——按推荐的代码模式（仅在 `fetchHomeData()` 入口递增）实现即天然正确。

3. **S8 BC 示例代码 token.value 检查逻辑（轻微）**: 影响限定于"可选增强"模块，核心修复独立交付不受影响。修复者可选择不实现此可选增强，或实现时使用独立的初始化标志替代 token.value 检查。
