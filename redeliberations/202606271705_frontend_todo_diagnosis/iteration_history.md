## 迭代第 1 轮

1. **问题描述**：诊断报告缺失具体修复建议，直接违反 requirement.md 明确要求的"修复建议"字段。报告末尾声明"不包含修复步骤或代码补丁"，但需求规格明确要求产出"问题清单（含严重程度、证据引用、修复建议）"。全部42个诊断条目均无"修复建议"字段。
   - 所在位置：a_v1_diag_v1.md 第519行（末尾声明）及第2节、第3节全部42个诊断条目
   - 严重程度：严重
   - 改进建议：在每个诊断条目末尾追加"修复建议"字段，至少包含：(a) 建议的修改文件和函数，(b) 关键实现逻辑简述，(c) 需注意的边界条件。对于"设计对齐"类问题（S8/S13），修复建议可指向设计文档更新而非代码修改。

2. **问题描述**：诊断报告对多个问题的严重程度重新定级，但与原始 todo.md 的定级不一致且未说明理由。例如 S3 从"严重"降级为"中"、G5 从"一般"变为"确认"但未建议调整结论。经质询核实，todo.md 中13个严重问题有10个被重新定级，差异远不止2例。无声的重新定级导致修复者以哪份文档的定级为准来决定修复顺序的问题。
   - 所在位置：a_v1_diag_v1.md 中 S3（第51-58行）、G5（第208-212行）及其他存在定级差异的条目
   - 严重程度：一般
   - 改进建议：(1) 对于与 todo.md 定级不同的条目，在诊断结论中显式说明重新定级的理由；(2) 对 G5 建议将诊断结论调整为"不成立（设计文档应更新以匹配更优实现）"；(3) 在总体评估中增加一行对比说明与原始定级的差异及原因。

3. **问题描述**：G14（API success 字段检查缺失）严重程度被系统性低估。该问题影响面覆盖全部7个 API 端点（useHomeApi.ts、useLifePlanApi.ts、usePunchApi.ts 共7个函数），后台返回 HTTP 200 但 success: false 时所有数据管道静默传递 null，可能导致运行时崩溃或白屏。诊断报告将其列为"一般问题"且放在优先级第4档，远低于其实际风险级别。
   - 所在位置：a_v1_diag_v1.md 中 G14（第263-268行），优先级排序（第511行将 G14 放在第4档）
   - 严重程度：一般
   - 改进建议：(1) 将 G14 严重程度上调至"中"或以上；(2) 明确需要修改的文件清单（至少3个 API composable 文件共约7个函数）；(3) 补充说明后端 API 契约中 success:false + HTTP 200 的发生场景；(4) 考虑在 useApi.ts 响应拦截器中统一处理。

4. **问题描述**：S8 诊断对 sessionStorage 短期缓解方案评估不完整，可能阻塞有意义的渐进式改进。诊断报告正确识别了 JWT Token localStorage 存储的 XSS 风险，但称"切换到 sessionStorage 只能降低非持久会话风险，无法根本解决 XSS 窃取问题"，忽略了 sessionStorage 相比 localStorage 的三项安全优势（不跨标签页共享、标签页关闭后自动清除、降低攻击面）。todo.md 给出的两步走策略（短期 sessionStorage + 长期 HttpOnly Cookie）是务实的渐进式方案，诊断报告将两者混为一谈可能导致修复者放弃短期可操作改进。
   - 所在位置：a_v1_diag_v1.md 中 S8（第109-119行），尤其第119行
   - 严重程度：一般
   - 改进建议：(1) 区分短期缓解（sessionStorage）和根治方案（HttpOnly Cookie）的有效性层级；(2) 明确 sessionStorage 虽不能根治但确实降低攻击面，建议作为过渡措施立即实施；(3) 补充 token 迁移到 sessionStorage 后 authStore 和路由守卫的联动修改点。

5. **问题描述**：修复优先级排序未标注问题间的依赖关系和批处理可行性。S6 修复依赖 S5 先完成（路由不存在时修复跳转目标将触发404），S1+S2 同属 sessionStorage 缓存模式可批处理，G7+G8+G12 均属抽取公共工具函数可一次重构完成，S3+S7 同在 Punch.vue + punchStore.ts 中修改区域重叠。不标注这些关系导致修复者按线性顺序工作产生不必要的往返和冲突。
   - 所在位置：a_v1_diag_v1.md 第8.2节（第508-515行），第7节根因分析（第450-487行）
   - 严重程度：一般
   - 改进建议：(1) 在优先级表中增加"前置依赖"列（如 S6→S5）；(2) 标注可批处理的问题组（S1+S2、G7+G8+G12、S3+S7）；(3) 在第7节根因分析与第8.2节优先级排序之间添加交叉引用映射关系。

## 迭代第 2 轮

1. **问题描述**：G5 汇总统计表与详细诊断结论存在逻辑矛盾——汇总表显示"不成立"为0，但 G5 详细诊断正文明确称"本问题不成立"，修订说明声称已调整但未实际同步更新汇总表和条目头部标签。导致汇总表与详细诊断之间的数据矛盾，修复者若只看汇总表会误判所有条目均需代码修改。
   - 所在位置：诊断报告第1节汇总表（第15-20行）、G5详细诊断条目头部（第334行）、G5正文第339行
   - 严重程度：一般
   - 改进建议：(1) 将 G5 条目头部诊断结论从"确认"改为"不成立（设计文档应更新以匹配更优实现）"；(2) 将第1节汇总表"不成立"行更新为 `| 不成立 | 0 | 1 | 1 |`，并将"确认"行从37调整为36；(3) 删除 G5 正文中"诊断结论调整"段落（属修订过渡期元注释，v2正式发布应直接呈现最终结论）。
2. **问题描述**：S8 sessionStorage 迁移修复建议遗漏关键代码修改点——修复建议仅显式列出 setToken/clearAuth/syncFromStorage 三个函数，但 authStore.ts 中 setAuth()（第39-41行）、fetchProfile()（第100-101行）、setProfile()（第108行）及初始化 ref 声明（第12-25行）中的 localStorage 操作点未被覆盖。按当前建议实施将导致 token 部分路径写 sessionStorage、部分路径写 localStorage，产生存储介质分裂。
   - 所在位置：诊断报告 S8 修复建议（第195-200行），对照代码 src/stores/authStore.ts 第12-113行全部 localStorage 操作
   - 严重程度：一般
   - 改进建议：(1) 补充完整的函数修改清单：setToken()、setAuth()、syncFromStorage()、clearAuth()、fetchProfile()、setProfile()、login()（仅 must_change_password 保留 localStorage），以及初始化 ref 声明；(2) 用表格列出每个函数中需修改和保留的 localStorage 键；(3) 增加说明"建议在 authStore.ts 中全文搜索 localStorage 并逐处判断是否迁移，确保无遗漏"。
3. **问题描述**：G14 严重程度说明行仍保留 v1 残留数字——严重程度行写有"约 7 个函数"，但下方详细证据列表逐条列出10个函数（useHomeApi 4个 + useLifePlanApi 4个 + usePunchApi 2个）。内部审议已指出但未修复，位于高度可见位置降低了报告的数字可信度。
   - 所在位置：诊断报告 G14 严重程度行（第436行）
   - 严重程度：轻微
   - 改进建议：将"约7个函数"改为"10个函数"，或删除数量描述改为"共涉及 3 个 API composable 文件中全部 API 函数"。
4. **问题描述**：缺少修复间交互风险评估——诊断报告 8.3 节仅覆盖4组单点副作用评估，未分析修复对之间的交叉影响。存在3组潜在交互冲突：(a) G14（success字段拦截器）与 S9（fetchAnalysis竞态保护）共享错误处理路径；(b) S8（sessionStorage迁移）与 S1/S2（sessionStorage缓存）共享命名空间和清理机制；(c) G7（Markdown渲染抽取）与 G14 共用错误处理链。
   - 所在位置：诊断报告 8.3 节（该节仅覆盖单点副作用，未覆盖修复间交互）
   - 严重程度：轻微
   - 改进建议：(1) 在 8.3 节末尾增加"修复交互风险"子节，列出上述3组交互及建议修复顺序；(2) 在 8.2 节优先级表中对存在交互风险的修复对增加脚注标注。
5. **问题描述**：所有42个条目均呈现为"需要某种行动"，缺少"无代码修改需求"条目的显式标注——G4（双模式并存可接受）、G5（代码优于设计）、S12（三元判断明确不需要代码修改）、S13（代码符合设计）四项实际诊断结论均为不需要代码修改，但在汇总表和优先级表中与需要实质代码修改的条目混编，修复者无法快速区分。
   - 所在位置：诊断报告第1节汇总表（第15-20行）、第8.2节优先级排序表（第780-796行）
   - 严重程度：轻微
   - 改进建议：(1) 在第1节汇总表中增加"无需代码修改"维度统计（G4/G5/S12/S13 共4个条目）；(2) 在第8.2节优先级表中增加"行动类型"列（代码修复/设计文档更新/仅确认）；(3) 在 8.1 节影响面分级中将"无需代码修改"条目从低危级中分离为独立行。

## 迭代第 3 轮

1. **问题描述**：G14 响应拦截器修复方案（方案B）与现有 getErrorMessage 函数不兼容。方案B中 `Promise.reject(new Error(...))` 产生的 Error 对象不含 `response` 属性，导致 `getErrorMessage()` 中 `'response' in err` 检查失败，后端真实错误消息被 fallback 字符串吞没，影响10个 API 函数的错误提示路径。
   - 所在位置：a_v3_diag_v1.md 第476-491行（G14 修复建议方案B代码块）、第858-864行（8.3(c) 未识别此断裂点）
   - 严重程度：一般
   - 改进建议：(1) 在 G14 方案B中构造类 AxiosError 对象，为 Error 实例附加 response 属性；(2) 或与 G8 合并修复，抽取 getErrorMessage 时增加纯 Error 的 message 提取分支；(3) 在 8.3(e) 交互风险表中增加 G14↔G8 条目。

2. **问题描述**：S5a 隐藏后端 API 依赖，优先级表将该问题标注为"无前置依赖"不准确。useHomeApi.ts 当前仅含 getDoctors、getArticles（列表）、getDiabetesTypes、getDiabetesType，不存在 getArticle(id) 单篇文章详情函数，需依赖后端接口就绪。
   - 所在位置：a_v3_diag_v1.md 第127行（S5a 修复建议）、第805行（优先级表）、第682行（技术可行性评估）
   - 严重程度：一般
   - 改进建议：(1) 优先级表 S5a"前置依赖"列标注"待确认：后端 GET /api/articles/:id 接口是否存在"；(2) 复杂度评估从"低"修正为"低至中，取决于后端 API 就绪状态"；(3) 明确 getArticle(id) 的预期请求/响应格式。

3. **问题描述**：S8 sessionStorage 迁移缺少跨标签页同步的具体方案。诊断报告仅以一句"如 BroadcastChannel"提及，无具体实现方案。sessionStorage 迁移后每个新标签页将因缺失 token 而强制跳转登录页，形成显著 UX 退化。
   - 所在位置：a_v3_diag_v1.md 第216行（S8 边界条件）
   - 严重程度：一般
   - 改进建议：(1) 在 S8 修复建议中增加"跨标签页同步（可选增强）"子节，给出 BroadcastChannel 最小实现方案；(2) 明确标注跨标签页同步为可选增强，核心修复可独立交付；(3) 在 8.3 节副作用评估中增加 S8 的跨标签页 UX 影响量化说明。

4. **问题描述**：S1/S2/S8 修复建议中缺少统一的缓存清理函数实现。8.3(e) 交互B虽已识别了 S8↔S1/S2 的清理依赖，但各修复建议中均未落实。修复者需跨章节整合信息才能实现完整清理。
   - 所在位置：a_v3_diag_v1.md 第200-212行（S8 函数修改清单）、第58-63行（S1）、第76-79行（S2）
   - 严重程度：轻微
   - 改进建议：(1) 在 S1/S2 修复建议中各增加一条暴露清理函数的说明；(2) 在 S8 的 clearAuth() 修改清单中增加调用上述清理函数。

5. **问题描述**：G15 修复建议的提示文案与 S7 修复后行为存在语义矛盾。G15 建议提示"分析基于所有打卡记录"，但 S7 修复后日期筛选变更触发重新拉取，分析数据基于筛选日期范围而非"所有记录"。
   - 所在位置：a_v3_diag_v1.md 第499行
   - 严重程度：轻微
   - 改进建议：将提示改为"分析基于当前筛选范围内的打卡记录"。

6. **问题描述**：G14（API success 字段检查缺失）未被任何根因覆盖。诊断报告第7节识别了5个系统性根因，但 G14 影响3个 API composable 文件共10个函数，具有系统性特征，却未被归入任何根因，削弱了根因分析的完整性。
   - 所在位置：a_v3_diag_v1.md 第726-772行（第7节根因分析）、766-772行（根因到问题映射表不含 G14）
   - 严重程度：轻微
   - 改进建议：(1) 新增根因6"缺乏统一的 API 响应处理规范"并映射 G14；(2) 或扩展现有根因1的范围以纳入 API 响应处理层面的系统性遗漏。

7. **问题描述**：8.3(b) 副作用评估的缓解措施与各修复建议之间脱节。8.3(b) 已准确识别 S1/S2 引入 sessionStorage 后缓存数据清理问题并提出缓解措施，但此信息仅存在于8.3节，S1/S2/S8 各自的修复建议正文中未体现此清理需求，修复者按优先级顺序实施时不会意识到需在 Store 中预留清理函数。
   - 所在位置：a_v3_diag_v1.md 第851-856行（8.3(b)）、第58-63行（S1）、第76-79行（S2）、第200-212行（S8）
   - 严重程度：轻微
   - 改进建议：(1) 在 S1/S2 修复建议末尾增加"与其他修复的交互"提示；(2) 在优先级表的 S1/S2 行脚注中指向 8.3(b) 的具体缓解措施。

## 迭代第 4 轮

1. **问题描述**：S5b 对 chatStore 和 SSE 机制的就绪状态未经实际代码验证。诊断报告称 S5b "依赖 chatStore 和 SSE 机制就绪"，但从未读取 src/stores/chatStore.ts 验证其状态。实际 chatStore 为骨架实现——conversations 为空 ref([])，abortActiveConnection() 为空函数体，SSE 相关类型完全未被引用。导致 S5b 工作量和复杂度被系统性低估（中高 → 高/极高），技术可行性评估"无阻塞性技术障碍"对 S5b 过于乐观。
   - 所在位置：第5节技术可行性评估 S5b 行、S5b 修复建议（第140-145行）、8.2节优先级表 S5b 行（第840行）、第7节根因分析（第762-768行）
   - 严重程度：严重
   - 改进建议：(1) 读取 src/stores/chatStore.ts 和 src/types/sse.ts，明确 chatStore 当前为骨架实现；(2) 将 S5b 拆分为 S5b-1（实现 chatStore SSE 核心）和 S5b-2（实现 DoctorChatView.vue + 路由）；(3) 调高 S5b-1 复杂度至"高"，在优先级表中标注 S5b-1 为独立 P0 项
2. **问题描述**：G18（缺少 AbortController）修复建议推荐"方案A（requestId 竞态快照）"，与 S9（fetchAnalysis 竞态保护）采用完全相同模式，但 G18 修复建议未引用 S9，未说明哪些 Store action 需补充保护。修复者可能在完成 S9 后误认为 G18 已解决。
   - 所在位置：G18 修复建议（第551-555行）、8.2节优先级表 S9/G18 行
   - 严重程度：一般
   - 改进建议：(1) 在 G18 修复建议中增加 S9 关联说明，明确需将 requestId 快照模式扩展到 homeStore.fetchHomeData()、lifePlanStore.fetchCurrent()、lifePlanStore.generate()、lifePlanStore.adjust()；(2) 在优先级表中增加互相脚注引用
3. **问题描述**：P2 优先级层内部（S9、S4+S11、S8、G14）缺少执行排序依据。S9 是 P1 中 S7 的前置依赖（S7 修复建议第174行明确要求 fetchAnalysis 需先有竞态保护），但此依赖未反映在优先级表中。按线性顺序处理可能导致 P1 修复时引入新竞态问题。
   - 所在位置：8.2节优先级表（第837-858行）、S7 修复建议（第174行）
   - 严重程度：一般
   - 改进建议：(1) P2 内部排序：S9 最先执行（解除 S7 依赖），G14 次之，S4+S11 和 S8 随后；(2) 将 S9 优先级上调至 P1 作为 S3/S7 前置项，或在 P1 的 S3+S7 行增加依赖注释
4. **问题描述**：S5a 识别了 useHomeApi.ts 中不存在 getArticle(id) 函数，但修复建议仅要求"确认后端 API 是否存在"，未提供后端不可用时的降级方案。存在可行的客户端降级路径（使用 getArticles() 全量拉取后客户端筛选），修复建议应提供 fallback 方案。
   - 所在位置：S5a 修复建议（第126-132行）、8.2节脚注 [†D]（第869行）
   - 严重程度：一般
   - 改进建议：(1) 增加降级方案：若后端接口不存在，用 getArticles() 全量拉取后客户端 find 筛选，包装为 getArticle(id) 函数；(2) 明确函数预期返回类型

## 迭代第 5 轮

1. **问题描述**：G18 修复建议中的 `pageInstanceId` 变量未说明声明位置和形式（let/ref），修复者无法直接照搬代码示例实施，若声明方式错误竞态保护将失效。
   - 所在位置：a_v5_diag_v7.md 第602-612行（G18 修复建议中的 page instance token 代码示例）
   - 严重程度：一般
   - 改进建议：在代码示例前增加前置说明，指出 `pageInstanceId` 应在 `homeStore.ts` 的 Store 函数体内（`useHomeStore()` 内部）、`fetchHomeData()` 函数外部，以 `let pageInstanceId = 0` 形式声明，同时引用 punchStore.ts:52 的 `const requestId = ref(0)` 作为参考模式。

2. **问题描述**：第5节"无阻塞性技术障碍"与第8.1节"阻塞级（影响功能可用性）"使用同一词汇"阻塞"表达技术可行性（实现层面）与用户可用性（功能层面）两个不同维度，未注明区分，可能导致读者困惑。
   - 所在位置：a_v5_diag_v7.md 第777行（5节结论）、第881行（8.1节分级标题）
   - 严重程度：一般
   - 改进建议：将8.1节的"阻塞级"改为"功能阻断级"或"用户不可用级"，与5节"技术障碍"明确区分；或在两处各增加简注说明"阻塞"在不同上下文中的含义。

3. **问题描述**：G4 在8.1节被列入"无需代码修改（仅确认）"、8.2节优先级表标注为"仅确认"，但G4修复建议正文（第411行）要求"在代码注释中标注"，属于代码修改操作，标签与实际修复行动矛盾。
   - 所在位置：a_v5_diag_v7.md 第897行（8.1节列表）、第925行（8.2节优先级表G4行）、第411行（G4修复建议正文）
   - 严重程度：一般
   - 改进建议：方案A：将G4行动类型改为"代码修复（注释）"，从8.1节"无需代码修改"列表中移出。方案B（推荐）：删除修复建议中的"加注释"要求，改为在设计文档中标注（纯文档更新），保持"仅确认"标签。

4. **问题描述**：8.3(e)和8.3(f)已识别修复间交互风险和累积风险，但缺少将风险转化为具体集成测试策略的章节，修复完成后的整体质量把关依赖修复者自行组织。
   - 所在位置：a_v5_diag_v7.md 第8.3节末尾至第8.4节之间（缺失内容）
   - 严重程度：一般
   - 改进建议：在8.3(f)后或8.4前增加"集成验证建议"子节，包含：(1) 核心用户路径端到端验证清单；(2) 交互风险A/B/C对应的专项验证点；(3) 跨标签页场景验证（S8 sessionStorage迁移后）；(4) 边界网络条件验证（慢网络/断网/401）。

5. **问题描述**：S5a 张力分析结论为"需与设计方/后端确认"，但未建议修复者先通过浏览器 DevTools 或 curl 直接验证后端 API 实际返回数据——这是比等待设计方回复更直接高效的信息获取路径。
   - 所在位置：a_v5_diag_v7.md 第125-128行（S5a 设计意图与类型约束的张力分析）
   - 严重程度：一般
   - 改进建议：在张力分析末尾增加操作建议："建议修复者实施前先通过浏览器 DevTools Network 面板或 curl 直接访问 GET /api/articles/:id，观察响应体中是否包含 content 字段。若包含则需新增 ArticleDetail 类型；若不包含则按降级方案实施。"

6. **问题描述**：8.2节优先级表仅有"复杂度"标签（低/中/高），缺少工时估算维度，不足以支撑迭代排期。
   - 所在位置：a_v5_diag_v7.md 第904-929行（8.2节优先级表）
   - 严重程度：轻微
   - 改进建议：在优先级表中增加"预估工时"列（可选，标注为粗略估计），至少对P0/P1级别的高复杂度条目给出数量级参考。


## 迭代第 6 轮

1. **问题描述**：版本号在文件名（a_v6_diag_v2）、主标题（v7）、元数据（v9）三处不一致，导致报告接收方无法确定当前版本。经质询确认三处版本标识不一致的事实仍然成立且未被v9修订解决。
   - 所在位置：文件名、诊断报告第1行主标题"# 前端代码审查问题诊断报告 v7"、第7行元数据"> **版本**: v9"
   - 严重程度：一般
   - 改进建议：(1) 将第1行主标题版本号修正为与元数据一致的"v9"；(2) 在文件名、主标题、元数据三个位置建立一致的版本标识约定，以元数据版本号为准同步更新。

2. **问题描述**：诊断分析深度在S级与G级之间存在系统性不对称——S1-S13及G14等高风险条目获得详尽的多段落分析（因果链、逐函数修改清单、边界条件、验证方法、代码示例），而G19-G23、G25-G27等15个P4级条目（占全部42项的1/3以上）仅获得2-4行简要描述，修复建议停留在原则层面（如G19未给出action重命名对照表、G20未指定具体ref合并清单、G22无代码定位）。修复者面对这些条目时缺乏"具体改哪里、改成什么"的操作指引。
   - 所在位置：诊断报告第3节G19（约第622-628行）、G20（约第630-636行）、G21（约第638-644行）、G22（约第646-652行）、G23（约第654-661行）、G25（约第675-681行）、G26（约第683-692行）、G27（约第694-700行）
   - 严重程度：一般
   - 改进建议：(1) 对G19：补充三个Store（homeStore/lifePlanStore/punchStore）全部action的当前命名与建议统一命名对照表；(2) 对G20/G21：给出具体的ref合并/拆分清单和修改后的Store接口签名；(3) 对G22：给出具体建议方向（统一为无参模式或标注为有意设计差异）；(4) 对G25/G26/G27：补充具体的代码修改位置（文件名+行号+旧代码+新代码对照）；(5) 若上述条目确实不值得投入详细修复建议，应在报告开头策略声明中明确"低危代码质量条目仅做问题识别，不提供逐项修复指引"。

3. **问题描述**：8.2节优先级表仅提供线性串行排序，缺少并行化策略和增量交付路径讨论。P0层硬前置项S5b-1（chatStore SSE核心，20-28h）与P1层独立项S1+S2（sessionStorage缓存，4-6h）之间无技术依赖，但报告未讨论：(a) P1独立项是否可与P0并行推进；(b) 团队其他成员在S5b-1由高级开发者独占实施期间可并行交付哪些独立项；(c) 是否存在增量交付路径（如先交付P1快速收益再逐步推进P0大项）。报告当前的线性优先级表隐式传达了逐项串行执行的策略。
   - 所在位置：诊断报告8.2节优先级表（约第913-940行）、预估工时汇总（约第926-928行）
   - 严重程度：一般
   - 改进建议：(1) 在8.2节末尾增加"并行化执行建议"子节，标注独立可并行的优先级组；(2) 标注P0内部子项并行可行性——S5a（2-4h）与S5b-1（20-28h）可完全并行；(3) 给出两种交付策略对比——串行保守策略与并行策略的工期估算；(4) 明确增量交付里程碑建议。


## 迭代第 7 轮

1. **问题描述**：G11 修复建议使用推测性行号定位（"推测在 ~158-180行区域"），降低可操作性。经实际代码读取，validateForm()函数位于LifePlan.vue:157-163，仅7行，推测范围与实际位置存在偏差。
   - 所在位置：诊断报告 G11 修复建议
   - 严重程度：一般
   - 改进建议：(1) 将推测性行号替换为精确行号"第157-163行"；(2) 修复建议中的代码片段与第158行、第160行实际校验条件逐行对应；(3) 对全部42个条目中涉及文件行号定位的修复建议，系统性地将"推测"/"约"/"~"等不确定性措辞替换为经实际代码读取确认的精确行号。若因文件版本不确定无法给出精确行号，应显式标注"行号基于当前快照，实施前请确认"。
2. **问题描述**：G24 page-enter 动画全局提取方案未分析两页面动画效果差异导致的统一化副作用。Home.vue的动画为opacity+translateY（淡入+上滑），LifePlan.vue的动画为纯opacity（纯淡入），两者效果不同。若统一提取为全局样式，必须选择一种作为全局定义，另一页面的动画效果将被改变。
   - 所在位置：诊断报告 G24 修复建议；8.3(e) 交互E（全局样式提取）
   - 严重程度：一般
   - 改进建议：(1) 在G24修复建议中增加"动画效果差异分析"子项，列出两页面当前动画差异；(2) 给出三种统一选项：选项A全局使用fadeIn+translateY，选项B全局使用纯fadeIn，选项C保留组件级覆盖；(3) 在8.3(e)交互E中补充动画效果差异的风险说明。
3. **问题描述**：S8 sessionStorage迁移的跨标签页UX退化在副作用评估中未量化影响面。迁移后每个新标签页（Ctrl+Click、外部链接、手动打开）因缺失token将强制跳转登录页，8.3(b)副作用评估识别了token失效后缓存残留风险，但未评估sessionStorage迁移本身造成的正向UX退化。
   - 所在位置：诊断报告 8.3(b) 副作用评估、S8 修复建议"跨标签页同步（可选增强）"
   - 严重程度：一般
   - 改进建议：(1) 在8.3(b)中增加"跨标签页UX退化量化"子项，列出受影响的三类用户场景及影响频次估计；(2) 明确建议将BroadcastChannel增强从"可选增强"升级为"强建议"；(3) 在8.3(g)跨标签页场景验证中标注UX退化项。
4. **问题描述**：G14两阶段部署策略缺少日志收集期间受影响UI路径的影响面分析。第一阶段日志收集期间，当前已存在的success:false静默null传递问题在哪些具体UI路径上持续发生未说明；GET /api/plan/current被提及但未给出若发现误报的处理预案。
   - 所在位置：诊断报告 G14 修复建议"分阶段部署建议（v12新增）"
   - 严重程度：一般
   - 改进建议：(1) 增加"日志收集期间的已知风险面"子项，列出受影响的UI路径，说明此期间风险敞口；(2) 补充"日志收集后决策树"，包括误报处理的排除逻辑；(3) 标注日志收集期建议时长，给出具体天数或版本窗口。
