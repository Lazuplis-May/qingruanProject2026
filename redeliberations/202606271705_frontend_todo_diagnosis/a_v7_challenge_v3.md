# 诊断质询报告（v3）

## 质询结果

LOCATED

## 逐维度审查

### 1. 证据充分性

**[通过]** 核心诊断证据链完整且可验证。对 S1/S2（sessionStorage 缓存缺失）的设计文档引用（第3466/3474/3482/3504行）与代码证据（homeStore.ts 无 sessionStorage 操作、LifePlan.vue 无 plan 缓存读写）形成闭合证据链。对 S3（默认日期）的代码证据（Punch.vue:22-23 `dateStart`/`dateEnd` 初始化为空字符串）经实际代码读取确认准确。对 S4（LifePlan 未读取 riskFormStore.result）的代码证据（LifePlan.vue 中 `result` 关键字全部指向 SweetAlert2 返回值而非 riskFormStore）经实际代码读取确认。对 S6（文章点击跳转 /news）的代码证据（Home.vue:82/85 `router.push('/news')`）经实际代码读取确认。对 G14（success 字段检查缺失）的代码证据（useApi.ts 响应拦截器无 success 检查）经实际代码读取确认。对 G24（page-enter 动画失效）的代码证据（Punch.vue:155 使用 `class="page-enter"` 但 scoped 样式中无对应动画定义）经实际代码读取确认。

**[通过]** S12 后端写入语义证据扎实。v12 新增的三处设计文档引用（3.2.16 节 HTTP 201 响应契约、响应体含服务端生成字段、6 节接口测试规范 curl + 数据库验证）经实际设计文档查阅确认存在且一致。HTTP 201 语义约定 + 响应体完整性 + 测试验证方法构成三重证据支撑，使"后端同步写入"前提从"未经验证假设"升级为"已有文档证据支撑"。

**[通过]** v11/v12 行号抽查验证范围合理。报告已对 12 处关键行号引用（S 类 9 处 + G 类 3 处）进行实际设计文档抽查，全部确认与当前快照一致。抽查覆盖了早期（v1-v3）确立的引用和后期（v9-v10）新增的引用，样本具有代表性。

**[问题-轻微]** 约50处行号引用（占全文60+处的约80%）仍未经过针对当前快照的系统性重新验证。虽然抽查样本全部通过且报告已采用"章节号+小节标题"作为稳定引用标识，但未被抽查的引用仍存在因设计文档修订导致行号漂移的残余风险。12/60 的抽查率提供了合理但不完全的置信度。建议在下一轮修订中将抽查范围扩展至 G 类剩余关键引用（如 G14 涉及的三处 API composable 行号引用、G18 的 `useApi.ts` 行号引用），达到 20+ 处抽查量后可将此条升格为"通过"。

**[通过]** G14 Error 对象兼容性审计覆盖全面。v12 新增的逐 Store 审计覆盖了 punchStore（3 个 catch 分支）、lifePlanStore（4 个 catch 分支）、homeStore（1 个 `Promise.allSettled` 路径），明确了 `instanceof Error` + `'response' in err` 的兼容链，结论可信。

### 2. 逻辑完整性

**[通过]** 因果链完整且可追溯。从问题现象（42 项 todo.md）到直接原因（代码偏离设计规定）到系统性根因（6 个根因，含映射表），形成了清晰的因果推导链路。根因 1（状态管理未对齐）解释了 S1/S2 的 sessionStorage 缓存系统性地缺失；根因 2（路由拆分未跟进）在 v9 修订后补充了 S5a（3 层缺失，2-4h）与 S5b（5 层缺失，36-52h）的完成度层级差异分析，避免了同质化概括可能导致的误判。

**[通过]** 优先级依赖关系明确且内部一致。8.2 节优先级表标注了硬性依赖（S6→S5a、S5b-2→S5b-1）、建议级依赖（S5b-1→G14）、批处理组（S1+S2、S3+S7、G7+G8+G12、G24+G25）和交互风险脚注（†A/†B/†C/†D）。v7 裁决了 S9 从 P2 上调至 P1 的优先级矛盾——消除了优先级标签与依赖关系的冲突。

**[通过]** 修复副作用评估体系完整。8.3 节从 (a) 路由守卫冲击、(b) 缓存清理策略、(c) 错误处理 UX 变化、(d) 公共函数回归范围、(e) 修复间交互风险（A-E 五组）、(f) 共享路径累积风险、(g) 集成验证建议七个维度进行了系统性评估。v10 新增的 D 行（Punch.vue 9 项修复聚合）和 E 行（全局样式提取）覆盖了此前遗漏的 P3/P4 层交叉影响。

**[通过]** S8 标签语义张力已在 v12 消除。诊断标签从"设计对齐"修正为"代码符合设计但设计存在安全缺陷——建议代码先行缓解，随后修订设计文档"，并附两步变更顺序说明（第一步 sessionStorage 迁移不影响"Web Storage 存储 Token"的技术选型大类，第二步更新设计文档），消除了修复者对变更顺序的困惑。

**[通过]** G14 分阶段部署策略已在 v12 补充。两阶段方案（日志收集期 → 切换为 reject）将一次性全局拦截的回归风险降至可控范围，特别标注了 `GET /api/plan/current` 的边界关注点。

**[问题-轻微]** S8"代码先行"论证的技术理由（sessionStorage 与 localStorage 同属 Web Storage API 家族，不改变技术选型大类）在技术分类层面成立，但在产品体验层面存在可讨论空间——从 localStorage 迁移至 sessionStorage 改变了用户登录的持久化行为（关闭标签页后需重新登录），这一变更的产品影响不亚于技术选型变更。报告在边界条件中已提及此 UX 影响（"用户关闭标签页后重新打开，token 丢失需重新登录——这是预期行为"），但"代码先行"的结论未充分讨论这一产品层面的权衡是否需要产品方确认。此条不影响根因定位的准确性，仅影响修复执行路径的决策依据。

**[问题-轻微]** 8.2.1 节三人并行策略（推荐）将关键路径定为 S5b-1（20-28h），工期估算 41-64h。但该估算隐含假设"S5b-1 的工作可被单人持续高效推进无阻塞"和"第二位开发者在 S5a（2-4h）完成后可无缝转入 S5b-2 协助"。若 S5b-1 所需的 fetch+ReadableStream SSE 管道实现经验集中在单一开发者身上，第二人转入时的 ramp-up 成本可能侵蚀并行收益。此条属执行规划层面的精细度问题，不影响诊断结论自身的正确性。

### 3. 覆盖完备性

**[通过]** 全部 42 项 todo.md 问题均获得逐项诊断。诊断覆盖了确认（37 项）、设计对齐（2 项）、部分确认（2 项）、不成立（1 项）四类结论，每项包含诊断结论、严重程度、设计依据、代码证据、因果链、影响范围、修复建议、验证方法八个字段。

**[通过]** 诊断范围声明已在 v12 明确。报告开头声明"以 todo.md 的 42 项问题为输入范围"，并给出两个已知遗漏示例（News.vue 的 sessionStorage 缓存要求第 3483 行、Risk.vue 的 sessionStorage 表单缓存），使读者对"42 项不等于全部设计偏差"有具体认知。此声明消除了此前 11 个迭代中潜在的覆盖范围误导。

**[通过]** 技术可行性维度覆盖完整。第 5 节对全部 42 项问题给出技术可行性评估，结论为"全部可实现，无阻塞性技术障碍"。v8 新增的术语说明区分了"技术可行性层面"的阻塞与"功能可用性层面"的阻断，消除了概念混淆。

**[通过]** 设计文档一致性检查（第 4 节）从功能遗漏、设计偏差、设计内部矛盾三个维度进行了交叉核验，覆盖了设计文档 1.2/1.6.1/4.1.2/4.1.4/4.1.8/4.2/4.3 等多个章节。

**[问题-轻微]** 8.2.1 节增量交付里程碑 M3（医生对话功能）将 S5b-1 + S5b-2 作为第 3-5 天的交付物，但 S5b-2 的修复建议中明确指出 Consultation.vue 重构（从 7 行占位到完整医生列表 UI）的复杂度和 S5b-1 的 chatStore SSE 核心实现同属"高"复杂度。若 Consultation.vue 的完整实现确实剥离为独立 Todo 项（如报告 S5b-2 复杂度评估所述），则 M3 的交付范围需重新界定（是"医生对话功能完整可用"还是"DoctorChatView 可用但需从 URL 直接进入"）。此条不影响诊断质量本身，但影响基于诊断报告的排期准确性。

## 质询要点

无严重或一般问题。上述 4 个轻微问题均不影响诊断结论的可信度和修复者的行动依据：

1. **行号引用抽查覆盖率为 20%（12/60+）**：抽查样本全部通过且具有代表性，报告已采用章节号作为稳定引用标识，残余风险低。建议扩展抽查至 20+ 处以进一步提升置信度。
2. **S8"代码先行"论证的产品层面权衡**：技术分类论证成立，UX 影响已在边界条件中明确说明，修复者可据此做出知情决策。
3. **三人并行策略的 expertise bottleneck 假设**：属于执行规划精细度问题，串行保守策略的 66-100h 估算已提供兜底基准。
4. **M3 里程碑的 Consultation.vue 范围界定**：报告在 S5b-2 诊断中已提供两种剥离方案的讨论，排期者可自行选择合适的范围界定。
