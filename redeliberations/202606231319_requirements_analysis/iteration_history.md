## 迭代第 1 轮

1. **问题描述**：users表缺少角色字段，无法支撑角色区分功能——第5节users表不含role字段，但第4.3节要求根据用户角色展示不同功能入口
   - 所在位置：第5节（数据需求）users表定义 vs 第4.3节（个人中心与登录注册）
   - 严重程度：严重
   - 改进建议：在users表中增加role字段（如TEXT CHECK (role IN ('user', 'admin'))），或在数据需求节同步更新表结构描述
2. **问题描述**：punch_in表缺少与life_plans的关联字段——第5节punch_in表无plan_id外键，但第4.5/4.7节明确将打卡绑定到具体方案项
   - 所在位置：第5节（数据需求）punch_in表定义 vs 第4.5节（生活方案）、第4.7节（打卡记录与分析）
   - 严重程度：严重
   - 改进建议：在punch_in表中增加plan_id字段并设置外键引用life_plans(id)；或同步调整功能描述为仅按类型统计
3. **问题描述**：缺少用户认证与会话管理机制——全文未涉及会话保持方式、Token机制、前后端鉴权流程、密码加密算法等基础架构问题
   - 所在位置：第4.3节及全文
   - 严重程度：一般
   - 改进建议：增加认证与安全章节，明确会话机制选型、Token生成/存储/传递/校验方式、前后端鉴权交互流程、密码加密算法
4. **问题描述**：life_advice表无对应功能模块——第5节定义了life_advice表但第3-4节无任何功能模块使用该表
   - 所在位置：第5节（数据需求）life_advice表 vs 第3-4节（功能模块）
   - 严重程度：一般
   - 改进建议：补充life_advice表对应的功能模块描述；或注明该表使用上下文，或评估移除
5. **问题描述**：缺少API接口规格说明——全文无系统化的API端点列表、请求/响应格式、错误码定义
   - 所在位置：全文
   - 严重程度：一般
   - 改进建议：增加API接口规格章节，至少列出核心端点、请求方法和简要数据结构

## 迭代第 2 轮

1. **问题描述**：密码处理方式前后矛盾 —— 第4.3节要求前端bcrypt哈希后传输，第4.10节推荐服务端哈希校验，两处方案互斥
   - 所在位置：第4.3节 vs 第4.10节
   - 严重程度：严重
   - 改进建议：统一为服务端哈希校验方案，修改第4.3节中"在前端经哈希处理后传输"的表述，改为"密码通过HTTPS传输至服务端，由服务端进行bcrypt哈希存储"
2. **问题描述**：数据操作路径存在全局性架构歧义 —— 第5节末尾声称所有数据库读写经Dify Text2SQL工作流，但第6节定义了常规Express CRUD端点，两条路径的边界未区分
   - 所在位置：第5节末尾说明 vs 第6节
   - 严重程度：严重
   - 改进建议：区分两类数据路径：AI驱动的数据操作（NL CRUD、预测结果写入）经Dify Text2SQL；常规CRUD（用户信息、打卡、文章）由Express直连SQLite
3. **问题描述**：医师对话API存在前端直连Dify与Express代理转发两种互斥方案
   - 所在位置：第4.2节 vs 第6.4节
   - 严重程度：一般
   - 改进建议：选定代理方案，修改第4.2节为"前端调用Express代理端点，由服务端附加chat_token后转发至Dify API"，chat_token不暴露给前端
4. **问题描述**：用户注册的实现路径与API定义不一致 —— 第4.3节描述经Text2SQL工作流，第6.1节定义标准Express端点
   - 所在位置：第4.3节 vs 第6.1节
   - 严重程度：一般
   - 改进建议：统一为常规Express操作，将第4.3节"注册信息通过Text2SQL工作流写入数据库"改为"注册信息通过Express接口写入数据库"
5. **问题描述**：认证边界未在页面/模块层面定义，全文未说明哪些页面需认证、哪些可公开访问
   - 所在位置：第4.3节验收标准
   - 严重程度：一般
   - 改进建议：新增"访问控制矩阵"段落，明确首页科普、医生列表、文章列表为公开访问，其余功能需登录
6. **问题描述**：缺少部署架构与服务器规划，原始需求中3台服务器的角色分配未在产出中体现
   - 所在位置：全文
   - 严重程度：一般
   - 改进建议：在第1节或第8节增加部署架构概述，说明数据服务器（Express+SQLite）与两台运行服务器（Nginx集群+静态资源）的角色分工
7. **问题描述**：健康资讯生成依赖的用户健康信息未明确来源与降级策略
   - 所在位置：第4.6节
   - 严重程度：一般
   - 改进建议：补充用户健康信息的数据来源（优先取user_risk_info表最新记录，其次取注册信息），以及无健康数据时的降级策略（全量展示分类标签）
8. **问题描述**：AI助手"健康建议"入口未同步到导航结构和个人中心入口列表
   - 所在位置：第4.8节 vs 第4.1节/第4.3节
   - 严重程度：一般
   - 改进建议：在4.3节入口列表中增加"健康建议"项，在4.1节导航说明中补充其层级归属
9. **问题描述**：Dify代理端点的请求体参数格式未定义，SSE流代理策略未说明
   - 所在位置：第6.4节、第6.8节、第6.10节
   - 严重程度：一般
   - 改进建议：为每个流式/代理端点给出核心参数名称和类型，说明SSE代理转发策略（透传或转换）
10. **问题描述**：风险预测表单步骤数未明确，前端无法确定采用分步向导还是单页表单
   - 所在位置：第4.4节
   - 严重程度：轻微
   - 改进建议：明确表单交互形式（推荐分步向导），验收标准增加"用户可自由前进/后退修改已填写内容"
11. **问题描述**：缺少医学免责声明功能描述，AI生成内容缺乏合规性界面元素
   - 所在位置：第4.2节/第4.4节
   - 严重程度：轻微
   - 改进建议：增加免责声明要求：首次使用AI功能前弹窗确认免责条款，AI生成内容底部展示固定免责提示文案

## 迭代第 3 轮

1. **问题描述**：第4.10节认证流程描述仍存在残余矛盾 — 正文主句声称"前端经bcrypt哈希后发送"，括号内又提出"服务端哈希校验"的替代方案并推荐后者，两个互斥方案并存，与第4.3节已统一的服务端哈希方案冲突。
   - 所在位置：第4.10节"认证流程"段落首句（第227行）
   - 严重程度：严重
   - 改进建议：将第4.10节"认证流程"段落首句修改为与4.3节一致的表述，建议改为："用户提交用户名和密码后，密码通过HTTPS安全通道传输至服务端，由服务端使用bcrypt算法进行哈希校验。具体流程：服务端对用户输入的密码计算bcrypt哈希后与数据库中存储的哈希值比对，匹配则生成JWT Token返回给前端。"删除括号内的备选方案描述。
2. **问题描述**：缺少系统初始数据的种子方案规范 — 系统启动后依赖预置数据（医生信息、糖尿病类型科普内容、管理员账号、科普文章）才能正常运行，但文档未定义这些数据的初始化方式。仅在第4.3节一句话提及管理员账号预置。
   - 所在位置：全文缺失（相关需求散见于4.1、4.2、4.3节和5节表定义，但无统一的初始化规范）
   - 严重程度：一般
   - 改进建议：在5节或7.4节末尾新增"初始数据要求"段落，至少明确：（1）diabetes_types表需预填充的糖尿病类型列表；（2）医生信息的初始化方式及chat_token的获取时机；（3）管理员账号的创建方式（建议数据库初始化脚本直接插入默认管理员记录，含bcrypt哈希后的默认密码，首次登录后强制修改）；（4）是否需要预置示例科普文章以填充首页。
3. **问题描述**：糖尿病类型科普功能缺少对应API端点 — 第4.1节将"糖尿病类型科普入口"列为首页功能，第5节定义了diabetes_types表，但第6节API接口规格中未定义任何读取糖尿病类型数据的端点。diabetes_types是唯一有数据表定义但无API端点的实体。
   - 所在位置：第4.1节与第5节之间存在功能定义和数据定义，但第6节缺失对应端点
   - 严重程度：一般
   - 改进建议：在6节新增"糖尿病类型"子节，定义端点：（1）`GET /api/diabetes-types` — 获取所有糖尿病类型列表；（2）`GET /api/diabetes-types/:id` — 获取单个类型的详细信息。两个端点可设为无需认证即可访问。
4. **问题描述**：Dify工作流清单分散各节，核心输入输出未集中定义 — 全文多处提及需创建的Dify工作流/Agent，但6.10节仅提供通用代理端点模板，未列出完整工作流清单及其核心inputs变量。各工作流的inputs定义分散在功能章节中，部分工作流甚至未提及任何inputs字段。
   - 所在位置：第6.10节（Dify工作流接口）+ 各功能章节（4.4-4.9）
   - 严重程度：一般
   - 改进建议：在6.10节末尾新增"Dify工作流清单"汇总表，包含：工作流标识推荐命名、用途、所属功能模块、核心inputs变量列表（字段名+类型+必填性）、触发端点。输入变量尚未明确的工作流至少标注"待概要设计阶段补充"。
5. **问题描述**：AI服务故障时的具体降级行为未定义 — 第7.3节"健壮性"仅有"AI接口异常时应有降级处理，不应导致页面崩溃或白屏"一句笼统要求，未说明任何具体的降级行为。考虑到平台几乎所有核心功能均依赖外部AI服务，缺少统一错误处理策略将导致各模块前端实现者自行决定降级行为，产生不一致的用户体验。
   - 所在位置：第7.3节（产品质量-健壮性）
   - 严重程度：一般
   - 改进建议：在7.3节补充具体降级规范：（1）AI服务不可用时展示统一错误状态组件（含提示文案和手动重试按钮）；（2）超时阈值统一为15秒，超时后展示提示并允许手动重试，不自动重试；（3）有历史数据缓存时优先展示缓存内容并标注生成时间；（4）SSE连接中断时展示重试提示条而非清空已有内容。
6. **问题描述**：对话会话(conversation_id)的生命周期管理未定义 — 第6.4和6.8节将conversation_id定义为可选请求参数，但缺少获取历史会话列表、恢复历史对话、新建会话触发方式、会话过期/清理机制等生命周期操作的定义。
   - 所在位置：第6.4节（医师咨询API）和第6.8节（AI助手API）的请求体参数表
   - 严重程度：轻微
   - 改进建议：在4.2节和4.8节各补充"会话管理"说明：（1）进入对话界面默认创建新会话；（2）localStorage维护当前活跃对话ID，跨页面返回自动恢复；（3）历史对话列表通过Dify会话列表API获取；（4）会话不设置自动过期，由用户手动删除。
7. **问题描述**：用户密码缺少强度要求规范 — 第4.3节定义了用户注册需提供用户名和密码，第4.10节定义了哈希存储方案，但全文未定义密码的最小长度、复杂度要求及前后端校验规则。
   - 所在位置：第4.3节（个人中心与登录注册）、第4.10节（用户认证与会话管理）
   - 严重程度：轻微
   - 改进建议：在4.3节注册描述中补充密码要求："密码长度不少于8位，需包含字母和数字"。

## 迭代第 4 轮

1. **问题描述**：`/api/admin/execute` 端点的权限模型与第5节双路径设计存在矛盾——第5节描述该端点可同时服务管理员和普通用户（经AI助手，通过行级权限约束），但第6.10节将其认证要求定义为"是 + admin"（要求 role=admin），两条规则互斥。该矛盾源于 v3_rev1 轮次对第5节数据路径进行双路径重构时未同步更新第6.10节认证列。
   - 所在位置：SRS 第5节"数据需求"中"AI驱动的Text2SQL路径"段落；第6.10节"管理相关"API表中 `POST /api/admin/execute` 的认证列
   - 严重程度：严重
   - 改进建议：方案A（推荐）：将 `/api/admin/execute` 的认证要求修改为"是"（JWT认证即可），在Express端点内部根据Token.role进行分级鉴权；或方案B：拆分为两个独立端点分别处理管理员和普通用户路径。同步更新第6.10节认证列和第4.10节管理员鉴权描述。
2. **问题描述**：users 表头像字段、doctor_information 表头像字段、diabetes_types 表图片字段、articles 表封面字段均已定义，但全文未定义图片上传API端点、存储策略、静态资源访问路径或文件类型/大小限制，前后端接口契约在头像功能上完全空白。
   - 所在位置：SRS 第5节数据需求（各表图片字段定义）；第6.2节用户相关API；第6节全文无上传端点
   - 严重程度：一般
   - 改进建议：明确头像/图片的存储策略，在第6节新增文件上传子节（至少定义头像上传端点），在7.4节部署架构中补充静态资源的Nginx托管路径说明。
3. **问题描述**：三个Express端点触发Dify工作流生成AI内容后需将结果持久化写入数据库（供后续GET端点返回），但"Dify响应解析→数据结构化→数据库INSERT"的关键流程从未被明确描述，属于未命名的第三条数据路径。
   - 所在位置：SRS 第5节数据路径定义；第6.3节（风险预测API）、第6.5节（生活方案API）、第6.7节（健康资讯API）
   - 严重程度：一般
   - 改进建议：在第5节增加第三条数据路径"AI内容生成持久化路径"，为每个涉及持久化的POST端点补充服务端处理流程简述，明确Dify工作流输出与数据库表字段的映射关系或标注留待概要设计阶段定义。
4. **问题描述**：AI助手Agent和管理员Agent以及各Dify工作流的API Key存储位置完全未定义。医生chat_token已建立数据库存储先例，但Agent/工作流API Key属于系统级配置，无法沿用同一模式，文档未给出替代方案。
   - 所在位置：SRS 第5节数据需求；第6.9-6.11节各代理端点
   - 严重程度：一般
   - 改进建议：在第5节或第7.4节明确系统级API Key通过环境变量或.env文件管理，给出推荐命名规范，在部署架构的环境依赖中补充Dify API Key清单。

## 迭代第 5 轮

1. **问题描述**：POST /api/risk/predict、POST /api/plan/generate、POST /api/articles/generate 三个核心AI端点的Express请求体参数完全未定义，实现者无法确定前端应发送的请求体JSON结构
   - 所在位置：产出第6.3节、第6.5节、第6.7节
   - 严重程度：严重
   - 改进建议：为三个POST端点各增加一份请求体参数表（格式参照第6.4节），至少列出Express端接收的JSON字段名、类型、是否必填。对于复杂嵌套对象展开列出内部字段；若部分字段需留待概要设计阶段细化，则至少给出顶层字段清单并标注"内部子字段待概要设计阶段补充"
2. **问题描述**：推荐EventSource API消费SSE流，但浏览器标准EventSource不支持自定义HTTP头，无法携带JWT Token完成认证，属于事实技术错误，且该错误通过引用传播到了AI助手端点
   - 所在位置：产出第6.4节第397行、第6.9节第454行
   - 严重程度：严重
   - 改进建议：删除EventSource API选项，明确仅推荐 fetch + ReadableStream 方式消费SSE流，补充技术说明解释原因
3. **问题描述**：PUT /api/plan/adjust、PUT /api/user/profile、PUT /api/user/password 三个端点请求体完全未定义，实现者无法编码
   - 所在位置：产出第6.5节（PUT /api/plan/adjust）、第6.2节（PUT /api/user/profile、PUT /api/user/password）
   - 严重程度：一般
   - 改进建议：为三个PUT端点各增加请求体参数表，明确字段名、类型、必填性和约束规则。PUT /api/user/profile与POST /api/upload/avatar的协作流程需以简要文字说明
4. **问题描述**：GET /api/risk/history 和 GET /api/articles/collections 缺少分页参数（page、pageSize），与其他列表端点不一致，数据量增长时存在性能隐患
   - 所在位置：产出第6.3节、第6.7节
   - 严重程度：一般
   - 改进建议：为这两个端点补充分页查询参数，与第6.12节定义的分页响应格式保持一致
5. **问题描述**：多医师场景下 conversation_id 在 localStorage 中的存储模型存在歧义——单一键存储会导致切换医生时覆盖原医生的会话ID上下文，文档未明确是按医生ID区分存储键还是有意设计为每次进入默认新会话
   - 所在位置：产出第4.2节"会话管理"段落
   - 严重程度：一般
   - 改进建议：在第4.2节末尾明确方案：建议localStorage按医生ID区分存储键，或明确"每次进入医生对话默认创建新会话"为有意设计并说明历史对话可通过会话列表恢复

## 迭代第 6 轮

1. **问题描述**：管理员智能管理功能缺少用户对话端点。第4.9节描述管理员通过自然语言对话界面操作数据库，但第6节API接口规格中管理员与admin-manager-agent之间的用户对话端点完全缺失。对比AI智能助手模块（`POST /api/assistant/chat` 与内部代理 `POST /api/dify/agent/:agent_id` 明确分离），管理员模块缺少等价端点。
   - 所在位置：缺失项，应位于第6.10节（管理相关API）
   - 严重程度：严重
   - 改进建议：在第6.10节新增管理员对话端点 `POST /api/admin/chat`（SSE流式响应，认证要求为"是+admin"），请求体参数至少包含message（string，必填）和conversation_id（string，可选），Express服务端代理转发至Dify admin-manager-agent。
2. **问题描述**：life_advice表的写入机制不完整。第4.8节描述AI助手可在对话中为用户生成健康生活建议并存储于life_advice表，但全文未定义Agent通过哪个Express端点写入life_advice表、Text2SQL路径如何保证字段完整性、或是否需要专用写入端点。对比health-article-generator工作流（第6.7节明确说明通过"AI内容生成持久化路径"写入articles表），life_advice的写入路径缺乏可执行的流程描述。
   - 所在位置：第4.8节AI智能助手功能描述、第6.11节Dify工作流清单汇总表diabetes-assistant-agent行
   - 严重程度：严重
   - 改进建议：在第5节"AI内容生成持久化路径"段落覆盖范围中明确纳入life_advice表，说明Agent写入life_advice的具体路径（推荐方案：在Dify Agent中配置专用HTTP工具回调Express端点执行INSERT，或明确走Text2SQL路径并说明Agent系统提示词中已约束生成符合life_advice表结构的SQL）。在第6.9节补充说明life_advice写入的具体端点或路径。
3. **问题描述**：Dify Agent Text2SQL工具回调中user_id动态传递的可行性未经验证。文档假设Dify平台支持在工具回调请求体中引用对话上下文的user参数，但未提供验证依据。
   - 所在位置：第5节"Dify Agent回调/api/admin/execute时的用户身份传递机制"段落
   - 严重程度：一般
   - 改进建议：在开发环境搭建阶段（项目阶段3任务1）增加验证任务，实测Dify平台Text2SQL工具是否能引用user参数进行回调请求体变量替换；若不通过则采用备选方案（服务端session→user_id映射表）。
4. **问题描述**：POST /api/admin/execute在Dify Agent回调场景下的请求体格式未定义。第5节和第6.10节仅从概念层面描述双认证模式，但JSON结构从未给出。
   - 所在位置：第6.10节/api/admin/execute端点
   - 严重程度：一般
   - 改进建议：为/api/admin/execute新增请求体参数表，明确字段名称、类型、必填性，推荐结构为`{ "query": "自然语言描述或SQL语句", "mode": "natural_language" | "raw_sql" }`。
5. **问题描述**：iframe间postMessage通信协议未定义，消息类型与payload格式完全空白。
   - 所在位置：第1.1节iframe跨模块通信机制
   - 严重程度：一般
   - 改进建议：在第1.1节末尾新增postMessage消息协议规范段落，至少定义AUTH_SYNC、NAVIGATE、DATA_TRANSFER三种核心消息类型的标准格式及event.origin校验方式。
6. **问题描述**：SSE流内Dify错误事件的客户端处理策略未定义。文档仅覆盖了SSE连接中断（网络层面），未覆盖流内逻辑错误事件。
   - 所在位置：第6.4节SSE代理转发策略、第6.9节SSE代理转发策略、第7.3节健壮性
   - 严重程度：一般
   - 改进建议：在第6.4节或第7.3节补充SSE流内错误事件处理策略，前端按event字段区分错误类型并展示对应提示，根据错误类型决定是否继续消费流。
7. **问题描述**：前端SPA路由结构完全未定义，URL路径、深层链接行为、浏览器前进后退、iframe URL结构、深度链接支持等问题均无答案。
   - 所在位置：缺失项，应位于第1节或第4.1节
   - 严重程度：一般
   - 改进建议：在第1节或4.1节新增前端路由结构段落，至少明确路由方案选型（hash/history）、各模块URL路径映射表、路由守卫逻辑、iframe子页面URL规范。
8. **问题描述**：JWT Token过期时的前端行为未定义，包括对话中输入消息时过期、多步骤表单中途过期、iframe过期同步等场景。
   - 所在位置：第4.10节用户认证与会话管理、第4.3节访问控制矩阵
   - 严重程度：一般
   - 改进建议：在第4.10节补充Token过期处理段落，统一拦截401响应、展示非阻断性提示、保留页面状态并支持登录后回跳恢复。
9. **问题描述**：Express端请求体字段名与Dify API参数名的映射关系未注明（如message vs query）。
   - 所在位置：第6.4节、第6.9节、第6.3节
   - 严重程度：一般
   - 改进建议：在第6节导言部分新增请求参数映射说明段落，明确Express代理层负责message→query等映射及工作流inputs变量名映射规则。
10. **问题描述**：页面/模块级别的交互状态（加载中、空数据、错误）未按模块系统定义。
   - 所在位置：第4.1-4.9节各功能模块描述、第7.3节健壮性
   - 严重程度：一般
   - 改进建议：在第7.1节或7.3节新增交互状态规范段落，统一加载中、空数据、错误三类中间状态的展示标准和展示层级。
11. **问题描述**：视觉设计系统基础规范（主题色、字体层级、间距尺度）未定义，缺少前端编写CSS所需的具体设计参数。
   - 所在位置：第7.1节用户界面需求
   - 严重程度：一般
   - 改进建议：在第7.1节补充设计系统基础参数子节，至少定义品牌主色调色值、字体族栈、字号层级表、基础间距尺度。
12. **问题描述**：多步骤表单的中间状态跨步骤持久化机制未明确，iframe架构下JavaScript内存持久化与"数据不丢失"承诺存在潜在矛盾。
   - 所在位置：第4.4节风险预测、第1.1节iframe跨模块通信机制
   - 严重程度：一般
   - 改进建议：在第4.4节补充表单数据跨步骤持久化说明，推荐使用sessionStorage存储，并标注iframe刷新/重载时的状态恢复策略。
13. **问题描述**：AI助手对话中健康建议生成的意图识别与触发机制不明确，文档仅描述"可以"但未说明"何时"触发。
   - 所在位置：第4.8节AI智能助手功能描述
   - 严重程度：轻微
   - 改进建议：在第4.8节或第6.11节补充触发机制说明，明确是由Agent基于对话上下文自主决策还是前端提供显式操作入口。
14. **问题描述**：前端模块的文件组织和构建方式未提及，HTML+CSS+JS技术选型在开发流程上存在模糊性。
   - 所在位置：缺失项，应位于第1节或第7.4节
   - 严重程度：轻微
   - 改进建议：在第1节或第8.1节补充前端技术选型说明，明确是否使用构建工具、第三方库引入方式、源代码推荐目录结构。
