# 再审议判定报告（v2）

## 判定结果

RETRY

## 判定理由

组件B第2轮诊断报告（`b_v2_diag_v2.md`）识别出8个质量问题，其中：

- **严重** 1个：API正常响应数据结构普遍缺失（问题1），直接阻碍前后端并行开发，使"需求可直接作为开发依据"的核心目标在当前文档状态下无法达成。
- **一般** 5个：验收标准时间指标缺乏测量起点定义（问题2）、腰围/收缩压"未提供"判定逻辑歧义（问题3）、多医生SSE连接管理策略未定义（问题4）、方案替换后打卡记录处理策略未定义（问题5）、user_risk_info表字段与API参数不完全对应（问题6）。
- **轻微** 2个：marked.js非标准包名（问题7）、Dify备选方案缺少关键实现细节（问题8）。

组件B质询报告（`b_v2_challenge_v2.md`）结论为 **LOCATED**，确认诊断报告中的8个问题证据充分、逻辑自洽、覆盖完备。质询过程仅发现一处轻微交叉引用断裂（"问题A"标签未定义），不影响诊断报告的整体可信度和可操作性。

组件B内部循环：最大轮次12轮，实际运行2轮（实际<最大），以LOCATED提前终止——审查结论被确认。

根据判定标准，诊断报告包含严重和一般等级的问题，满足RETRY条件。

## 需要解决的问题

- **问题描述**：API正常响应数据结构普遍缺失——POST /api/auth/login、POST /api/risk/predict、GET /api/doctors、POST /api/plan/generate、GET /api/articles/:id、POST /api/assistant/chat（SSE）等核心端点的响应体JSON schema均未定义
- **所在位置**：第6节各子节（6.1-6.10）
- **严重程度**：严重
- **改进建议**：为核心POST端点补充响应体JSON schema示例；为GET列表端点补充data数组元素字段定义；补充SSE事件data字段结构说明；或在第6节导言统一声明留待概要设计阶段补充并标注为待办事项

- **问题描述**：验收标准中的时间指标（3秒/10秒/15秒/1秒）均未定义计时起点，开发者无法确定从哪个事件开始计时、以何种方式测量
- **所在位置**：4.2节（第209行）、4.4节（第252行）、4.5节（第283行）、4.6节（第296行）、4.8节（第338行）、4.9节（第357行）
- **严重程度**：一般
- **改进建议**：为每个含时间指标的验收标准补充计时起点定义，推荐统一采用用户触发操作的时刻；区分前端计时和服务端计时场景；对依赖外部AI服务的指标标注合理预期而非硬性约束

- **问题描述**：腰围/收缩压"未提供"与"填写了0"的判定边界未定义，前端校验规则和服务端估算触发条件存在歧义
- **所在位置**：第4.4节第234行、第6.3节第608-609行
- **严重程度**：一般
- **改进建议**：明确"未提供"判定规则（null/undefined/空字符串触发估算，0视为无效输入）；在API参数说明中补充有效范围和校验错误码；前端添加placeholder提示

- **问题描述**：多医生场景下SSE连接管理策略未定义——切换医生时旧连接是否关闭、连接数上限、后台对话消息接收和缓存策略均未说明
- **所在位置**：第4.2节第205-206行、第6.4节第631行
- **严重程度**：一般
- **改进建议**：补充SSE连接管理策略（切换医生时关闭当前连接、仅保持当前可见对话活跃连接、后台对话通过历史会话恢复）

- **问题描述**：方案调整后新方案"替换"旧方案，但旧方案关联的打卡记录处理策略未定义——物理删除将导致打卡记录plan_id悬空，逻辑过期需新增is_active字段
- **所在位置**：第4.5节第265-267行、第6.5节第657行、第4.7节第302-303行、第5节第478行
- **严重程度**：一般
- **改进建议**：明确方案替换策略，推荐采用逻辑过期（新增is_active字段）而非物理删除

- **问题描述**：user_risk_info表缺少diabetes_history字段的明确存储方案（独立列还是JSON字段）；第4.6节引用了表中不存在的BMI字段
- **所在位置**：第5节第475行、第6.3节第598-611行、第4.6节第288行
- **严重程度**：一般
- **改进建议**：在user_risk_info表中明确diabetes_history存储方式；将BMI引用修正为身高和体重

- **问题描述**：marked.js使用了非标准npm包名，官方包名为marked
- **所在位置**：第291行、第992行
- **严重程度**：轻微
- **改进建议**：将marked.js统一改为marked

- **问题描述**：Dify平台能力验证的备选方案缺少关键实现细节，备选方案依赖与主方案相同的{{user}}变量透传机制
- **所在位置**：第5节第494行
- **严重程度**：轻微
- **改进建议**：补充备选方案的session_id传递和映射表生命周期管理细节，或声明留待概要设计阶段补充
