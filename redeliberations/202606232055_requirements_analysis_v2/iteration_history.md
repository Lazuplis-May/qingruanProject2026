## 迭代第 1 轮

1. **问题描述**：第1节前端技术栈声明未被修订——第1节第1段（文档第5行）仍保留"平台以HTML、CSS、JavaScript为前端技术栈"，未按修订指令修改为"Vue 3 + TypeScript + Vite"。
   - 所在位置：第1节"项目定位与目标"，文档第5行
   - 严重程度：严重
   - 改进建议：将第1节首段中的"HTML、CSS、JavaScript"替换为"**Vue 3 + TypeScript**"，并在同句中补充"使用 **Vite** 作为构建工具"。

2. **问题描述**：前端架构存在全局性二义性——文档同时描述了两套互斥的前端架构：（A）iframe子页面模式+postMessage跨模块通信协议；（B）Vue Router SPA架构+路由映射表。两套架构无法共存，开发者无法确定应按哪种模式开始编码。
   - 所在位置：第1节第9行（iframe声明）、第1.1节第10-28行（postMessage协议）、第1.2节第30-77行（Vue Router SPA声明）
   - 严重程度：严重
   - 改进建议：（1）做出明确技术决策，选择Vue Router SPA（推荐）或iframe多页面架构；（2）若选择Vue Router SPA：删除1.1节全部iframe通信机制描述、删除第1节iframe嵌入说明、修正4.10节交叉引用；（3）若保留iframe架构需说明micro-frontend模式和实施成本。

3. **问题描述**：第16.14节修订记录存在遗留矛盾——v6→v7修订记录记载"采用原生HTML/CSS/JavaScript开发"，与当前第8.1节正文的Vue3+Vite描述矛盾。历史修订记录未被同步清理，当前修订轮次缺少对应的修订说明条目。
   - 所在位置：第16.14节，文档第1596-1601行
   - 严重程度：一般
   - 改进建议：（1）在16.14节追加说明该条内容已在后续修订中被替换；（2）新增当前修订轮次的修订说明条目；（3）同步清理16.12节中残留的iframe同源前提描述。

4. **问题描述**：第9节澄清说明第1条仍以iframe为基线架构进行解释——"基于项目任务文档中明确的'底部Tab栏+iframe子页面'架构"，与Vue Router SPA架构选择相矛盾。
   - 所在位置：第9节澄清说明第1条，文档第933行
   - 严重程度：一般
   - 改进建议：将澄清说明第1条修正为反映Vue3 SPA架构，或标注此澄清仅适用于SRS v1的原始架构假设。

5. **问题描述**：第4.10节登录态共享描述包含指向iframe架构的互斥交叉引用——正文正确描述了Pinia authStore统一登录态管理，但末尾写有"具体实现方式见1.1节"，而1.1节是postMessage登录态广播（iframe架构机制），与Pinia方案互斥。
   - 所在位置：第4.10节，文档第310行
   - 严重程度：一般
   - 改进建议：删除"具体实现方式见1.1节"的交叉引用；若iframe架构被废弃则直接删除，若保留则需明确说明两种方案的适用场景及关系。

6. **问题描述**：第16.12节修订记录残留iframe架构前提描述——记载"依赖所有iframe页面同源部署的前提条件"，但4.4节当前活动内容已正确使用Vue3 SPA术语，修订历史记录未同步更新。
   - 所在位置：第16.12节，文档第1587行
   - 严重程度：一般
   - 改进建议：在16.12节对应修订记录中追加说明，标注该前提描述已在后续修订中被移除。

7. **问题描述**：修订记录庞大但核心修订遗漏——7轮54个问题修复但最基础的修订要求（第1节技术栈声明修改）未被执行，反映修订过程缺乏对修订指令清单的逐项核对。
   - 所在位置：贯穿第10-16节修订说明
   - 严重程度：一般
   - 改进建议：增加一次针对修订指令清单的逐项核对（checklist-based verification），确保每一条修订指令都有对应的修订操作和位置记录。

8. **问题描述**：TypeScript代码示例覆盖不足——全文仅第1.2节有一处TypeScript示例，修订指令第2条要求"所有代码示例统一为TypeScript"的覆盖度不足以验证完成程度。
   - 所在位置：全文代码示例分布
   - 严重程度：一般
   - 改进建议：在4.10节Pinia authStore、1.2节路由守卫、第6节API接口等处补充TypeScript示例，或在修订说明中明确声明需求文档阶段代码示例的定位。

9. **问题描述**：第7.4节部署架构中"HTML/CSS/JS静态文件"术语存在上下文歧义——三处使用该术语描述Vite构建产物，虽技术层面正确，但在全文修订目标为消除原生开发描述的背景下存在被解读为手工编写模式的歧义风险。
   - 所在位置：第7.4节，文档第823、827、839行
   - 严重程度：一般
   - 改进建议：将"HTML/CSS/JS静态文件"统一修改为"Vite构建产物（含HTML/JS/CSS静态资源）"。

10. **问题描述**：第9节澄清说明第11、12条的残余iframe引用未被单独标注——第11条提及"iframe间数据传递"，第12条提及"iframe间登录态共享"，与Vue3 SPA修订方向存在潜在歧义。
   - 所在位置：第9节，第953行（第11条）和第955行（第12条）
   - 严重程度：轻微
   - 改进建议：在统一清理iframe残余描述时同步更新此两处措辞，使其反映Vue3 SPA架构修订方向。

## 迭代第 2 轮

1. **问题描述**：API正常响应数据结构普遍缺失——POST /api/auth/login、POST /api/risk/predict、GET /api/doctors、POST /api/plan/generate、GET /api/articles/:id、POST /api/assistant/chat（SSE）等核心端点的响应体JSON schema均未定义
   - 所在位置：第6节各子节（6.1-6.10）
   - 严重程度：严重
   - 改进建议：为核心POST端点补充响应体JSON schema示例；为GET列表端点补充data数组元素字段定义；补充SSE事件data字段结构说明；或在第6节导言统一声明留待概要设计阶段补充并标注为待办事项
2. **问题描述**：验收标准中的时间指标（3秒/10秒/15秒/1秒）均未定义计时起点，开发者无法确定从哪个事件开始计时、以何种方式测量
   - 所在位置：4.2节（第209行）、4.4节（第252行）、4.5节（第283行）、4.6节（第296行）、4.8节（第338行）、4.9节（第357行）
   - 严重程度：一般
   - 改进建议：为每个含时间指标的验收标准补充计时起点定义，推荐统一采用用户触发操作的时刻；区分前端计时和服务端计时场景；对依赖外部AI服务的指标标注合理预期而非硬性约束
3. **问题描述**：腰围/收缩压"未提供"与"填写了0"的判定边界未定义，前端校验规则和服务端估算触发条件存在歧义
   - 所在位置：第4.4节第234行、第6.3节第608-609行
   - 严重程度：一般
   - 改进建议：明确"未提供"判定规则（null/undefined/空字符串触发估算，0视为无效输入）；在API参数说明中补充有效范围和校验错误码；前端添加placeholder提示
4. **问题描述**：多医生场景下SSE连接管理策略未定义——切换医生时旧连接是否关闭、连接数上限、后台对话消息接收和缓存策略均未说明
   - 所在位置：第4.2节第205-206行、第6.4节第631行
   - 严重程度：一般
   - 改进建议：补充SSE连接管理策略（切换医生时关闭当前连接、仅保持当前可见对话活跃连接、后台对话通过历史会话恢复）
5. **问题描述**：方案调整后新方案"替换"旧方案，但旧方案关联的打卡记录处理策略未定义——物理删除将导致打卡记录plan_id悬空，逻辑过期需新增is_active字段
   - 所在位置：第4.5节第265-267行、第6.5节第657行、第4.7节第302-303行、第5节第478行
   - 严重程度：一般
   - 改进建议：明确方案替换策略，推荐采用逻辑过期（新增is_active字段）而非物理删除
6. **问题描述**：user_risk_info表缺少diabetes_history字段的明确存储方案（独立列还是JSON字段）；第4.6节引用了表中不存在的BMI字段
   - 所在位置：第5节第475行、第6.3节第598-611行、第4.6节第288行
   - 严重程度：一般
   - 改进建议：在user_risk_info表中明确diabetes_history存储方式；将BMI引用修正为身高和体重
7. **问题描述**：marked.js使用了非标准npm包名，官方包名为marked
   - 所在位置：第291行、第992行
   - 严重程度：轻微
   - 改进建议：将marked.js统一改为marked
8. **问题描述**：Dify平台能力验证的备选方案缺少关键实现细节，备选方案依赖与主方案相同的{{user}}变量透传机制
   - 所在位置：第5节第494行
   - 严重程度：轻微
   - 改进建议：补充备选方案的session_id传递和映射表生命周期管理细节，或声明留待概要设计阶段补充

## 迭代第 3 轮

1. **问题描述**：Dify 平台 `{{user}}` 变量透传能力未实证验证即作为核心架构基础——核心架构假设（AI-to-database 授权管道依赖 Dify Agent 工具回调中 `{{user}}` 的动态透传）在未经验证的情况下被直接用作架构设计前提，且该验证被推迟至开发环境搭建阶段。若验证未通过，主方案和备选方案均无法工作，存在显著返工风险。
   - 所在位置：第 5 节"Dify 平台能力验证任务"段落（文档第 627 行附近）
   - 严重程度：严重
   - 改进建议：（1）将 Dify 平台能力验证前置为门禁任务，在概要设计启动前完成；（2）在验证完成前明确标注所有依赖此能力的功能模块（4.8 AI 智能助手的 Text2SQL 操作、4.9 智能管理）为"待 Dify 平台能力验证后确认的技术方案"；（3）补充验证未通过时的完整替代架构方案（如 Express 代理层在转发 Dify Agent 请求前预查询用户数据并注入对话上下文）。

2. **问题描述**：7x24 可用性承诺与 SQLite 单实例架构存在不可调和的事实矛盾——第 7.2 节要求"系统应保持 7x24 小时连续运行能力"，但第 7.4 节同时承认 SQLite 为文件型数据库，不具备主从复制或自动故障转移能力，服务器宕机时 API 服务和数据访问将全部中断。SQLite 单实例架构无法满足 7x24 可用性 SLA，且文档未给出宕机恢复时间目标（RTO）。
   - 所在位置：第 7.2 节"性能"第 4 条（文档第 1182 行）与第 7.4 节"SQLite 数据可靠性说明"（文档第 1228 行）
   - 严重程度：严重
   - 改进建议：（1）将"7x24 小时连续运行能力"修改为符合 SQLite 单实例架构实际可用性的表述，或明确标注 RTO；（2）在第 7.4 节补充与服务可用性声明的交叉引用，明确标注当前架构下的可用性约束是已知的架构取舍；（3）若实训项目对可用性无硬性要求，建议直接删除 7x24 声明。

3. **问题描述**：Dify 工作流 `health-article-generator` 的 `user_id` 参数在专用端点中的传递路径未显式说明——POST /api/articles/generate 作为专用端点，其服务端处理流程仅笼统描述"调用 Dify health-article-generator 工作流"，未显式说明 Express 如何将 JWT 中的 user_id 映射为工作流 inputs 中的 `user_id` 字段。通用代理端点的说明不适用于专用端点。
   - 所在位置：第 6.7 节 POST /api/articles/generate 端点与第 6.11 节 Dify 工作流清单汇总表
   - 严重程度：一般
   - 改进建议：（1）在 6.7 节服务端处理流程中显式补充 JWT user_id → Dify 工作流 inputs.user_id 的映射步骤；（2）在第 6 节导言"请求参数映射说明"表格中增加 JWT Token.user_id → Dify 工作流 inputs.user_id / Dify Agent `user` 参数的映射约定行；（3）同步检查其他依赖 JWT user_id 自动填充的专用端点。

4. **问题描述**：通用 Dify 代理端点与专用代理端点的职责边界未定义——两组端点的关系、调用路径（前端是否直接调用通用端点）、专用端点是否内部复用通用端点逻辑、通用端点是否仅供内部使用等问题均未明确，影响实现决策和代码复用。
   - 所在位置：第 6.11 节"通用 Dify 代理端点"与第 6.4/6.9/6.10 节各专用端点
   - 严重程度：一般
   - 改进建议：（1）在第 6.11 节明确通用代理端点的定位，建议标注为"Express 内部代理层使用的通用转发端点，由各专用端点内部调用，不直接暴露给前端"；（2）更新 Dify 工作流清单汇总表的"触发端点"列，对由专用端点间接触发的工作流标注实际的专用端点路径；（3）在架构层面明确分层：前端 → 专用 API 端点 → 通用 Dify 代理端点 → Dify API。

5. **问题描述**：修订说明章节存在逆时序排列——第 17 节（v1→v2，最早修订批次）被插入在第 16 节（v6→v7）和第 18 节（v7→v8）之间，破坏时间线一致性，实现者按章节顺序阅读会产生"前面刚定义、后面又删除"的困惑。
   - 所在位置：第 10-18 节修订说明（文档第 1385 行起）
   - 严重程度：一般
   - 改进建议：（1）将所有修订说明按时间线从旧到新排列（v1→v2 至 v7→v8）；（2）在修订说明总起段前增加阅读指引。

6. **问题描述**：Dify Agent 工具链定义完全推迟至概要设计阶段——Agent 能调用哪些工具、每个工具的输入输出 schema、权限边界等信息直接影响前端交互设计，当前在需求阶段未列出任何 Agent 预期工具清单，意味着前后端开发者进入概要设计时仍需返回需求层面做大量澄清。
   - 所在位置：第 6.11 节 Dify 工作流清单汇总表、第 4.8 节 AI 智能助手、第 4.9 节智能管理
   - 严重程度：一般
   - 改进建议：（1）在需求文档中至少列出每个 Agent 的"预期工具清单"，标注每个工具的用途、输入输出类型和权限约束；（2）为每个工具标注对前端交互的影响类型；（3）若确实无法确定完整清单，至少应明确工具定义的产出时间节点和责任人。

7. **问题描述**：枚举字段的具体字符串值在全文中未统一约定——`diabetes_history`、`family_history`、`gender` 等枚举字段在三处描述的精度不一致，前后端开发者无法独立确定枚举值的具体字符串（中文还是英文）、是否需要中英文映射、是否需要白名单校验。
   - 所在位置：第 4.4 节风险预测、第 6.3 节 API 参数表、第 5 节 user_risk_info 表
   - 严重程度：一般
   - 改进建议：（1）在第 6.3 节各枚举参数说明中明确约定枚举值字符串（推荐与 Dify 工作流 inputs 变量保持一致），标注"其他值将被服务端拒绝"；（2）在第 5 节表定义为枚举字段补充 CHECK 约束或枚举值说明；（3）同步检查 `family_history`、`gender` 等类似字段。

8. **问题描述**：前端 UI 组件库选型缺失——移动端优先的 Web 应用全文未涉及 UI 组件库选型（如 Vant、Element Plus、Naive UI 等），导致设计系统参数无法与组件库主题变量对接、移动端特有交互组件（Tabbar、ActionSheet、DatetimePicker 等）的实现方式悬而未决、开发者可能误认为所有 UI 组件需手动实现。
   - 所在位置：第 8.1 节"在范围内"、第 7.1 节"用户界面"
   - 严重程度：一般
   - 改进建议：（1）在第 8.1 节明确前端 UI 组件库选型（推荐 Vant 4 作为移动端优先的 Vue3 组件库）；（2）在第 7.1 节设计系统参数中补充与所选组件库主题变量的映射关系；（3）若推迟选型至少应在 8.1 节声明"待概要设计阶段确定"并列出评估维度。
